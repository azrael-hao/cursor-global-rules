---
description: 数据相关决策必须基于真实数据，禁止使用预估、理论值或猜测进行判断
globs:
alwaysApply: true
---

# 数据驱动决策原则

## 核心原则

**所有涉及数据的决策、判断、优化必须基于真实有效的数据，严禁使用预估值、理论值或主观臆断。**

## 🔴 强制要求（MANDATORY）

### 必须使用真实数据的场景

1. **性能优化决策**
   - ❌ 禁止："这个查询应该很快"
   - ✅ 必须：执行EXPLAIN ANALYZE获取实际执行时间

2. **数据规模判断**
   - ❌ 禁止："表数据量不大，应该没问题"
   - ✅ 必须：SELECT COUNT(*) 获取实际行数

3. **容量规划**
   - ❌ 禁止："预计每天增长1万条"
   - ✅ 必须：查询实际历史增长数据

4. **索引效果评估**
   - ❌ 禁止："加索引应该能提升性能"
   - ✅ 必须：对比加索引前后的实际执行计划

5. **资源使用评估**
   - ❌ 禁止："内存应该够用"
   - ✅ 必须：查看实际内存使用监控数据

6. **批量操作影响**
   - ❌ 禁止："这个批量更新应该很快"
   - ✅ 必须：先在小范围测试，记录实际耗时

7. **数据分布分析**
   - ❌ 禁止："数据应该是均匀分布的"
   - ✅ 必须：查询实际数据分布统计

8. **并发影响判断**
   - ❌ 禁止："理论上支持1000并发"
   - ✅ 必须：实际压测获取并发能力

## 📊 如何获取真实数据

### 数据库相关

```sql
-- 表行数
SELECT COUNT(*) FROM table_name;

-- 表大小
SELECT pg_size_pretty(pg_total_relation_size('table_name'));

-- 索引使用情况
SELECT * FROM pg_stat_user_indexes WHERE tablename = 'table_name';

-- 查询执行计划
EXPLAIN ANALYZE SELECT ...;

-- 数据分布
SELECT column_name, COUNT(*) 
FROM table_name 
GROUP BY column_name 
ORDER BY COUNT(*) DESC;
```

### 性能指标

```bash
# 查询监控数据
# CPU使用率、内存使用率、磁盘IO
# 响应时间、QPS、错误率

# 日志分析
# 慢查询日志、错误日志、访问日志
```

### 数据抽样

- 生产环境：查询实际数据统计
- 测试环境：使用生产数据样本（脱敏后）
- 压测环境：模拟真实负载

## ❌ 禁止的表达方式

### 预估性词汇（NEVER USE）

- "应该"、"大概"、"估计"、"可能"
- "理论上"、"按照经验"、"一般来说"
- "差不多"、"大约"、"预计"、"推测"

### 错误示例

```
❌ "这个表数据量不大，不需要分页"
→ 没有查询实际行数

❌ "加个索引应该能解决慢查询"
→ 没有分析执行计划

❌ "按照每天1000条增长，一年才36万条"
→ 没有查询实际增长趋势

❌ "内存16G应该够用了"
→ 没有监控实际内存使用

❌ "这个接口理论QPS能到5000"
→ 没有实际压测数据
```

## ✅ 正确的决策流程

### 1. 数据收集阶段

```
问题：这个查询是否需要优化？

步骤1: 获取当前数据
- 查询表行数
- 查看执行计划
- 记录实际响应时间

步骤2: 分析瓶颈
- 是否全表扫描？
- 是否缺少索引？
- 是否JOIN过多？

步骤3: 验证优化效果
- 在测试环境验证
- 对比优化前后数据
- 确认性能提升百分比
```

### 2. 容量规划阶段

```sql
-- 查询历史增长数据
SELECT 
    DATE(creation_time) as date,
    COUNT(*) as daily_count
FROM table_name
WHERE creation_time >= NOW() - INTERVAL '30 days'
GROUP BY DATE(creation_time)
ORDER BY date;

-- 基于真实数据推算
当前总量: 1,234,567 条
最近30天平均增长: 15,234 条/天
预计一年增长: 15,234 * 365 = 5,560,410 条
```

### 3. 性能优化阶段

```
优化前:
- 执行时间: 2.3秒（实际测试）
- 扫描行数: 500,000行（EXPLAIN分析）
- 返回行数: 100行

优化后:
- 执行时间: 0.08秒（实际测试）
- 扫描行数: 100行（使用索引）
- 性能提升: 96.5%（2.3s → 0.08s）
```

## 🎯 强制验证清单

在做任何数据相关决策前，必须回答：

- [ ] 我是否查询了实际数据量？
- [ ] 我是否分析了实际执行计划？
- [ ] 我是否记录了实际性能指标？
- [ ] 我是否基于监控数据做判断？
- [ ] 我的结论是否有具体数字支撑？

**任何一项为"否"→ 不能做决策，必须先获取数据**

## 特殊场景

### 新系统/新功能

对于尚未上线的系统：
- ✅ 可以：基于相似系统的实际数据
- ✅ 可以：基于业务预测 + 压测验证
- ❌ 禁止：纯理论推算 + 不验证

### 紧急故障

紧急情况下可以先处理，但必须：
1. 明确标注"临时方案，未经数据验证"
2. 事后补充数据验证
3. 根据实际数据调整方案

## 📝 决策记录模板

```markdown
## 问题
[描述需要决策的问题]

## 数据收集
- 当前表行数: 1,234,567 (实际查询)
- 平均查询时间: 2.3秒 (最近7天监控)
- 峰值QPS: 1200 (最近30天最大值)

## 分析结论
基于以上真实数据，判断：
[数据支持的结论]

## 优化方案
[基于数据的方案]

## 预期效果
[基于测试环境验证的效果]
```

---

**记住：数据驱动 > 经验驱动 > 理论驱动。没有数据，就没有发言权。**
