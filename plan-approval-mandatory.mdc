---
description: 🔴 CRITICAL - 方案计划执行前强制确认 - 禁止未经用户同意直接执行方案
globs:
alwaysApply: true
---

# 🔴 方案计划执行前强制确认（PLAN APPROVAL MANDATORY）

## 核心原则

**任何方案、计划、解决方案在实际执行前，必须先征得用户明确同意。严禁制定方案后立即执行，绕过用户审核环节。**

## 🚫 严禁的行为

### 1. 制定方案后直接执行

```
❌ 错误流程：
1. 分析问题
2. 制定解决方案
3. 直接开始执行（错误！）

✅ 正确流程：
1. 分析问题
2. 制定解决方案
3. 使用AskQuestion征求用户确认
4. 用户同意后再执行
```

### 2. 假设用户会同意

```
❌ 错误思维：
"这个方案很合理，用户肯定会同意，直接执行吧"

✅ 正确思维：
"即使方案很合理，也必须先让用户确认，可能用户有其他考虑"
```

### 3. 边说边做

```
❌ 错误示例：
"我将采用方案A，现在开始修改文件..."
（同时已经在调用工具修改文件）

✅ 正确示例：
"我建议采用方案A，理由是...，是否确认执行？"
（等待用户确认后再调用工具）
```

### 4. 分步执行绕过确认

```
❌ 错误做法：
"我先执行第一步，然后再问第二步..."
（将完整方案拆分，逐步执行以绕过整体确认）

✅ 正确做法：
"完整方案包含3个步骤：...，是否确认执行全部步骤？"
（展示完整方案，一次性确认）
```

## ✅ 正确的方案执行流程

### Step 1: 问题分析

```
清楚地描述问题：
- 当前状态是什么？
- 期望状态是什么？
- 问题的根本原因是什么？
```

### Step 2: 方案制定

```
提供完整的解决方案：
- 方案描述：具体要做什么
- 实施步骤：分几步，每步做什么
- 影响评估：会影响哪些文件/功能
- 风险评估：可能的风险和应对
- 替代方案：如果有其他选择，一并列出
```

### Step 3: 征求确认（MANDATORY）

**必须使用AskQuestion工具**：

```javascript
AskQuestion({
  questions: [{
    id: "plan_approval",
    prompt: "## 问题分析\n[问题描述]\n\n## 解决方案\n[方案详情]\n\n## 实施步骤\n1. ...\n2. ...\n\n## 影响评估\n[影响范围]\n\n是否确认执行此方案？",
    options: [
      { id: "approve", label: "确认执行" },
      { id: "modify", label: "需要调整方案" },
      { id: "reject", label: "不执行，另想办法" }
    ]
  }]
})
```

### Step 4: 根据用户选择执行

```
用户选择"确认执行" → 开始执行方案
用户选择"需要调整方案" → 询问调整意见，重新制定方案
用户选择"不执行" → 停止，询问用户的期望方向
```

## 📋 需要强制确认的场景（MANDATORY）

以下场景**必须**在执行前征求用户确认：

### 1. 代码修改方案

```
场景：需要修改代码解决bug或实现功能

必须确认的内容：
- 哪些文件会被修改
- 修改的具体内容
- 是否影响现有功能
- 是否需要测试
```

### 2. 数据库变更方案

```
场景：需要修改数据库schema或数据

必须确认的内容：
- 哪些表会被修改
- 具体的DDL/DML语句
- 是否需要备份
- 是否可回滚
```

### 3. 架构调整方案

```
场景：需要调整系统架构或技术选型

必须确认的内容：
- 架构调整的理由
- 新旧架构对比
- 迁移成本和风险
- 实施计划
```

### 4. 文件操作方案

```
场景：需要删除、移动、重命名文件

必须确认的内容：
- 哪些文件会被操作
- 操作的理由
- 是否有引用
- 是否需要备份
```

### 5. 依赖变更方案

```
场景：需要添加、升级、删除依赖

必须确认的内容：
- 哪些依赖会变更
- 变更的理由
- 版本兼容性
- 是否影响其他依赖
```

### 6. 配置修改方案

```
场景：需要修改配置文件

必须确认的内容：
- 哪些配置会变更
- 变更的理由
- 是否影响现有功能
- 如何回滚
```

### 7. 批量操作方案

```
场景：需要批量修改多个文件

必须确认的内容：
- 影响的文件列表
- 每个文件的修改内容
- 批量操作的风险
- 如何验证结果
```

### 8. 重构方案

```
场景：需要重构代码结构

必须确认的内容：
- 重构的范围
- 重构的理由
- 重构的步骤
- 如何保证功能不变
```

## 🎯 不同类型方案的确认要求

### 类型1：简单方案（单文件小修改）

```
确认要求：
- 简要说明修改内容
- 提供修改前后对比
- 使用AskQuestion确认

示例：
"建议修改config.json中的port从8080改为3000，是否确认？"
```

### 类型2：中等方案（多文件或逻辑复杂）

```
确认要求：
- 详细说明方案内容
- 列出所有影响的文件
- 说明修改理由和影响
- 使用AskQuestion确认

示例：
"方案包含3个文件修改：
1. server.js - 调整端口配置
2. client.js - 更新API地址
3. docker-compose.yml - 更新端口映射
是否确认执行？"
```

### 类型3：复杂方案（架构变更或高风险）

```
确认要求：
- 先切换到Plan模式制定详细方案
- 提供完整的方案文档（问题、方案、步骤、影响、风险）
- 如有多个方案，列出对比
- 使用AskQuestion确认
- 考虑分阶段执行，每阶段确认

示例：
"建议采用微服务架构替换单体架构：
【问题】：...
【方案】：...
【步骤】：...
【影响】：...
【风险】：...
【替代方案】：...
是否确认执行？"
```

## 🚨 特殊场景处理

### 场景1：紧急修复

```
即使是紧急修复，也必须确认！

正确做法：
1. 说明紧急情况
2. 提供快速修复方案
3. 强调紧急性
4. 使用AskQuestion快速确认

❌ 错误："这是紧急bug，我直接修复了"
✅ 正确："发现紧急bug，建议立即修复，方案是...，是否确认？"
```

### 场景2：用户明确说"帮我做XXX"

```
即使用户说"帮我做"，也要先展示方案再执行！

正确做法：
1. 理解用户需求
2. 制定具体方案
3. 展示方案给用户
4. 确认后执行

❌ 错误：用户说"帮我删除这个文件" → 直接删除
✅ 正确：用户说"帮我删除这个文件" → "我将删除xxx文件，已确认无引用，是否执行？"
```

### 场景3：方案很简单/很明显

```
即使方案很简单，也必须确认！

理由：
- 用户可能有其他考虑
- 可能存在你不知道的约束
- 尊重用户的决策权

❌ 错误："这个修改很简单，直接改了"
✅ 正确："建议做这个简单修改：...，是否确认？"
```

### 场景4：之前类似操作用户同意过

```
即使之前同意过类似操作，本次也要确认！

理由：
- 每次情况可能不同
- 用户的想法可能改变
- 不能假设用户的意图

❌ 错误："上次你同意删除类似文件，这次我也删了"
✅ 正确："与上次类似，建议删除xxx文件，是否确认？"
```

### 场景5：分多步执行的方案

```
如果方案分多步，有两种确认方式：

方式1：整体确认（推荐）
- 展示完整的多步方案
- 一次性确认全部步骤
- 按顺序执行

方式2：分步确认（仅当步骤间有不确定性时）
- 展示第一步方案并执行
- 根据第一步结果，展示第二步方案
- 每步都确认

❌ 禁止：先执行一步，然后说"接下来我将..."就继续执行
```

## 📝 方案确认自检清单（MANDATORY）

在执行**任何**方案前，必须完成以下检查：

```
=== 方案执行前自检 ===

【方案信息】
- [ ] 我已清楚描述了问题
- [ ] 我已制定了完整的解决方案
- [ ] 我已列出了所有实施步骤
- [ ] 我已评估了影响范围
- [ ] 我已评估了风险

【确认状态】
- [ ] 我已使用AskQuestion征求用户确认
- [ ] 用户已明确选择"确认执行"
- [ ] 我理解用户的确认意图

【执行准备】
- [ ] 我知道从哪里开始
- [ ] 我知道每一步要做什么
- [ ] 我知道如何验证结果

=== 检查完毕，可以执行 ===
```

**如果"确认状态"中任何一项为"否" → 不能执行，必须先征求确认**

## 💡 正确示例

### 示例1：简单修改的确认

```
用户："帮我把端口改成3000"

AI思考过程：
1. 这是一个简单的配置修改
2. 需要修改config.json文件
3. 虽然简单，但也必须确认

AI回复：
"我将修改config.json，把端口从8080改为3000：

```json
{
  "port": 3000  // 原值：8080
}
```

这个修改会影响服务器启动端口。是否确认执行？"

[使用AskQuestion工具]

用户确认后 → 执行修改
```

### 示例2：复杂方案的确认

```
用户："优化数据库查询性能"

AI思考过程：
1. 这是一个复杂任务，需要分析和多步操作
2. 必须先切换Plan模式制定方案
3. 方案制定后必须确认

AI回复：
[切换到Plan模式]

"经过分析，建议采用以下优化方案：

## 问题分析
当前查询耗时5秒，主要瓶颈是全表扫描。

## 优化方案
1. 在user_id字段添加索引
2. 修改查询语句使用索引
3. 添加查询缓存

## 实施步骤
步骤1：创建索引
```sql
CREATE INDEX idx_user_id ON orders(user_id);
```

步骤2：修改查询代码
[具体代码修改]

步骤3：配置Redis缓存
[具体配置]

## 影响评估
- 索引创建需要约2分钟，期间表会锁定
- 查询性能预计提升80%
- 需要重启应用以加载新配置

## 风险评估
- 索引会占用约100MB磁盘空间
- 缓存可能导致数据延迟（最多5秒）

是否确认执行此优化方案？"

[使用AskQuestion工具]

用户确认后 → [切换到Agent模式] → 执行优化
```

### 示例3：分步确认的正确做法

```
用户："清理项目中的无用文件"

AI回复：
"我需要分两步进行：

## 第一步：扫描分析
扫描项目，识别可能的无用文件，生成清理清单供您审核。
这一步是只读操作，不会删除任何文件。

是否确认执行第一步？"

[用户确认第一步]

[执行扫描]

"扫描完成，发现以下可能无用的文件：
1. old_backup_20230101.sql (2年未修改)
2. test_temp.js (无引用)
3. deprecated_module.py (标记为废弃)

## 第二步：删除文件
根据扫描结果，建议删除上述3个文件。

是否确认执行第二步（删除文件）？"

[用户确认第二步]

[执行删除]
```

## 🚨 违规示例

### 违规示例1：直接执行

```
❌ 错误：

用户："优化这个查询"
AI："好的，我已经添加了索引并修改了查询语句。"
[同时已经执行了修改]

问题：
- 没有展示方案
- 没有征求确认
- 直接执行了数据库修改
```

### 违规示例2：假设用户同意

```
❌ 错误：

AI："我发现了性能问题，最好的解决方案是添加缓存层。
我现在就来实现..."
[开始写代码]

问题：
- 没有征求用户确认
- 假设用户会同意这个方案
- 可能用户有其他优先级
```

### 违规示例3：边说边做

```
❌ 错误：

AI："我将采用方案A，首先修改config.js..."
[同时调用工具修改文件]

问题：
- 在描述方案的同时就开始执行
- 用户还没来得及审核方案
- 违反了"先确认后执行"原则
```

## 🎓 与其他规则的关系

### 与 decision-change-approval 的区别

```
decision-change-approval: 
- 侧重于"选择"
- 有多个方案时，让用户选择哪个

plan-approval-mandatory:
- 侧重于"执行"
- 方案制定后，必须确认才能执行
- 即使只有一个方案，也要确认

两者互补，都必须遵守！
```

### 与 plan-before-implementation 的关系

```
plan-before-implementation:
- 侧重于"何时规划"
- 复杂任务必须先规划后实施

plan-approval-mandatory:
- 侧重于"执行前确认"
- 规划完成后，必须确认才能执行

流程：复杂任务 → 切换Plan模式规划 → 征求确认 → 执行
```

## 📊 确认流程总结

```
┌─────────────┐
│ 分析问题    │
└──────┬──────┘
       ↓
┌─────────────┐
│ 制定方案    │
└──────┬──────┘
       ↓
┌─────────────────────────┐
│ 使用AskQuestion征求确认 │ ← MANDATORY
└──────┬──────────────────┘
       ↓
   ┌───┴───┐
   │用户选择│
   └───┬───┘
       ↓
  ┌────┴────┐
  │ 确认?   │
  └─┬─────┬─┘
    │     │
   是│    │否/调整
    ↓     ↓
┌────────┐ ┌──────────┐
│执行方案│ │重新制定  │
└────────┘ └────┬─────┘
                 ↓
           (返回制定方案)
```

---

**记住：方案再好，未经确认不执行。尊重用户的决策权，这是AI助手的基本准则。**

**核心口诀：先制定，再展示，征求确认，后执行。无确认，不动手。**
