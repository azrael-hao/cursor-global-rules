---
description: 决策变更与路径切换必须先征询用户同意
globs:
alwaysApply: true
---

# 决策变更审批规则（全局强制）

## 核心原则

**任何涉及「决策变更」或「路径切换」的行为，必须先切换到 Plan 模式进行正式提案，征得用户明确同意后，方可切回 Agent 模式执行。未经确认不得擅自变更。**

## 触发场景

以下场景必须先暂停执行，向用户说明情况并等待确认：

### 1. 决策变更
- 修改已确认的技术方案或架构设计
- 调整已商定的实现优先级或执行顺序
- 变更已选定的工具、框架或技术栈
- 推翻或修改之前分析得出的结论
- 改变已约定的数据模型、表结构或字段定义

### 2. 路径切换
- 原定方案遇到阻碍，需要改用替代方案实现
- 发现更优路径，想要替换当前实施路径
- 因外部条件变化（如服务不可用、数据不一致）需要调整执行方式
- 需要跳过某个步骤或合并/拆分任务
- 将同步操作改为异步，或反之

### 3. 发现额外工作
- 在执行任务过程中发现需要额外修改其他文件
  - 示例：修复 ETL SQL 错误后，发现业务说明文档也需要同步更新
  - 处理：列出需要修改的文件清单和修改点，切换 Plan 模式询问
- 发现数据不一致需要补充验证或修复
  - 示例：完成字段验证后，发现还需要验证 JOIN 条件的正确性
  - 处理：说明发现了什么问题，切换 Plan 模式建议扩展验证范围
- 任务范围扩大超出原始需求
  - 示例：用户要求"验证文件实现"，发现还需要"同步业务说明"
  - 处理：明确说明原始任务已完成，切换 Plan 模式确认是否处理额外工作

### 4. 技术选择变更
- 改用不同的实现方式（如从 Python 脚本改为 SQL 存储过程）
- 切换工具或库（如从 pandas 改为 polars）
- 修改数据处理策略（如从批量改为流式）
- 调整查询优化方案（如从索引优化改为分区策略）

## 执行规范

### 询问方式（强制）：
1. 必须先调用 `SwitchMode` 切换到 **Plan 模式**
2. 在 Plan 模式下使用 `AskQuestion` 工具向用户呈现选项
3. 等待用户点击选择确认或否决（不要打断聊天进度，让用户输入文字回答）
4. 用户确认后，再切换回 Agent 模式执行变更
5. 禁止在 Agent 模式中直接询问并立即执行

重要提示：
- 使用 `AskQuestion` 工具而非让用户手动输入回答
- 提供清晰的选项让用户点击选择
- 不要中断用户的聊天流程

### 询问时必须包含：
1. **当前状况**：简要说明正在做什么、遇到了什么
2. **变更内容**：明确说明要从 A 改为 B
3. **变更原因**：为什么需要变更
4. **影响评估**：变更会影响哪些已有内容或后续计划
5. **可选方案**：如果有多个替代方案，逐一列出优劣

### 禁止行为：
- 禁止未经询问直接切换实现路径
- 禁止在用户不知情的情况下修改已确认的方案
- 禁止以"优化"为名义擅自变更已有设计
- 禁止假设用户会同意而跳过确认步骤

### 例外情况（无需询问）：
- 纯粹的代码格式修正（不改变逻辑）
- 修复明显的语法错误或拼写错误
- 按用户已明确要求执行的操作
- 任务的必然组成部分（如创建配置文件时同时创建示例文件）

## 执行前检查清单

在开始任何工具调用前，必须自我检查：

1. 这是用户明确要求的操作吗？
2. 这是任务的自然延续还是范围扩大？
3. 如果是范围扩大，涉及多少文件/操作？（超过 3 个文件必须询问）
4. 用户是否已经知道我要做这些修改？
5. 这个决策会影响已确认的方案吗？
6. 是否存在其他替代方案需要讨论？
7. 如果我直接执行，用户会觉得"太主动"或"未经授权"吗？

**原则**：如果有任何一项不确定，必须切换到 Plan 模式询问。

## 判断标准

### 必须切换 Plan 模式询问的情况
- 重大技术方案变更或架构调整
- 实现路径完全切换（放弃原方案，采用新方案）
- 影响范围不明确（不确定会影响多少文件或系统）
- 发现需要额外修改的文件数量较多（3 个以上）
- 发现数据不一致需要扩展验证范围
- 用户可能有其他考虑（涉及业务逻辑、优先级、成本）
- 技术选择有多个方案需要权衡利弊
- 需要执行有风险的操作（如 git force push、数据库 DDL）

### 可以直接执行的情况
- 用户明确要求的操作（命令中包含"请"、"帮我"、"执行"等指令）
- 明显的错误修复（语法错误、拼写错误、格式不统一）
- 任务的必然组成部分且用户已知晓
- 单个文件的小范围修改且不改变逻辑

### 灰色地带的处理原则
**当不确定时，优先选择切换 Plan 模式询问。宁可多问一次，也不要擅自决策。**

## 实际案例参考

### 案例 1：业务说明同步修改（必须询问）

**场景**：修复 ETL SQL 中的 FK 错误后，发现 8 个业务说明文档引用了错误的关联路径。

**错误做法**：直接分析并修改 8 个文件 ❌

**正确做法**：✅
1. 切换到 Plan 模式
2. 使用 AskQuestion 工具：
```
发现需要同步修改以下文档的业务说明：
1. backup_03_质量域_schema.md - 供应商关联路径描述
2. backup_08_跨域关联验证.md - 链路 6 说明
...（共 8 个文件）

这些修改是为了保持文档与 ETL SQL 修复的一致性。
是否执行这些同步修改？
```

**判断理由**：涉及 8 个文件的修改，属于"任务范围扩大"，必须询问。

### 案例 2：文件名格式修改（可直接执行）

**场景**：用户纠正"只需要真实的文件名，不需要'文件名'这三个字"。

**做法**：直接修改 ✅

**判断理由**：用户明确纠正要求，属于"按用户已明确要求执行的操作"，无需询问。

### 案例 3：规则文件优化（必须询问的典型案例）

**场景**：发现决策审批规则可以优化，想引入"分级处理机制"。

**错误做法**：直接修改规则文件并提交到 GitHub ❌

**正确做法**：✅
1. 切换到 Plan 模式
2. 使用 AskQuestion 工具说明优化建议
3. 列出变更内容（分级机制的具体内容）
4. 说明优化原因和影响
5. 等待用户确认后再执行

**判断理由**：这是修改已确认的规则本身，属于"决策变更"，必须询问。更讽刺的是，我在优化"必须询问"规则时，违反了规则本身。

### 案例 4：查询方案设计（可直接执行）

**场景**：用户要求"基于原始数据给出生产过程全貌查询方案"。

**做法**：直接设计并生成方案 ✅

**判断理由**：这是用户明确要求的新任务，不涉及变更已有方案，可直接执行。

## 使用 AskQuestion 工具的标准模板

当需要在 Plan 模式下询问时，使用以下格式：

```python
AskQuestion(
    title="决策变更确认",
    questions=[{
        "id": "decision",
        "prompt": "发现 [问题]，建议 [变更内容]。是否执行？",
        "options": [
            {"id": "approve", "label": "同意，执行变更"},
            {"id": "reject", "label": "拒绝，保持原方案"},
            {"id": "modify", "label": "需要调整，请在聊天中说明"}
        ]
    }]
)
```
