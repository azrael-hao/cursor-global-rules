---
description: 🔴 CRITICAL - 所有用户选择、判断、提问必须使用AskQuestion工具 - 严禁聊天中列选项
globs:
alwaysApply: true
---

# 🔴 强制使用AskQuestion工具规则（ALL USER INTERACTIONS MUST USE ASKQUESTION）

## 核心原则（MANDATORY）

**所有涉及用户选择、判断、提问、确认的场景，必须使用AskQuestion工具。严禁在聊天中以任何形式列出选项。**

## 🚫 绝对禁止的行为

### 禁止在聊天中列出选项（任何形式）

```
❌ 错误示例1：字母选项
"您想要如何处理？
A. 立即为cj-datart创建构建脚本
B. 先验证cj-dinky是否有问题"

❌ 错误示例2：数字选项
"请选择：
1. 使用方案A
2. 使用方案B
3. 放弃这个任务"

❌ 错误示例3：列表形式
"有以下几个选择：
- 选项1：修改配置
- 选项2：重构代码
- 选项3：保持现状"

❌ 错误示例4：问句形式
"您是选择方案A还是方案B？"
"要执行吗？(是/否)"

这些全都是严重错误！必须使用AskQuestion工具！
```

## 📋 必须使用AskQuestion的场景（MANDATORY - 全覆盖）

### 1. 方案选择（任何时候有2个以上选项）
```
场景：有多种技术方案、实现路径、解决思路

强制要求：
- 2个以上方案 → 必须AskQuestion
- 即使有明显最优方案 → 仍然AskQuestion
- 即使用户没明确要求选择 → 仍然AskQuestion

禁止：
- "我建议选择方案A"
- "方案B更好，我采用方案B"
- 任何形式的自作主张选择
```

### 2. 执行确认（任何需要用户同意的操作）
```
场景：
- 执行方案前的确认
- 删除文件的确认
- 修改配置的确认
- 数据库变更的确认
- 重构代码的确认

强制要求：
- 所有方案执行前 → 必须AskQuestion确认
- 所有高风险操作前 → 必须AskQuestion确认
```

### 3. 用户偏好询问（不确定用户想法时）
```
场景：
- 不确定用户要A还是B
- 不确定功能优先级
- 不确定实现细节
- 不确定是否需要某个功能

强制要求：
- 任何不确定 → AskQuestion询问
- 不要猜测用户意图
```

### 4. 超时重新确认
```
场景：AskQuestion超时后重新提问

强制要求：
- 超时 → 重新AskQuestion
- 标注"重新确认"
```

### 5. 业务决策
```
场景：
- 优先级排序
- 成本权衡
- 功能取舍
- 性能 vs 可读性权衡

强制要求：
- 所有业务决策 → AskQuestion
```

### 6. 歧义澄清
```
场景：
- 需求有多种理解
- 缺少关键信息
- 不清楚用户真实意图

强制要求：
- 有歧义 → AskQuestion澄清
```

### 7. 路径选择
```
场景：
- 下一步有多个可能方向
- 需要决定优先处理什么

强制要求：
- 有多个方向 → AskQuestion让用户选择
```

### 8. 风险评估后的决策
```
场景：
- 操作有风险，需要用户权衡
- 不确定用户是否接受风险

强制要求：
- 有风险 → 说明风险 → AskQuestion确认
```

## ✅ 正确使用AskQuestion的方法

### 标准格式

```javascript
AskQuestion({
  title: "标题（可选）",
  questions: [{
    id: "question_id",           // 唯一标识
    prompt: "问题描述",           // 清晰的问题
    options: [
      { id: "option1", label: "选项1描述" },
      { id: "option2", label: "选项2描述" },
      { id: "option3", label: "选项3描述" }
    ],
    allow_multiple: false         // 是否允许多选（可选，默认false）
  }]
})
```

### 示例1：方案选择

```javascript
AskQuestion({
  questions: [{
    id: "approach",
    prompt: "发现数据库性能问题，建议以下优化方案：\n\n方案A：添加索引（快速，风险低）\n方案B：分库分表（彻底，工作量大）\n方案C：使用缓存（中等，需维护）\n\n请选择优化方案：",
    options: [
      { id: "a", label: "方案A - 添加索引" },
      { id: "b", label: "方案B - 分库分表" },
      { id: "c", label: "方案C - 使用缓存" }
    ]
  }]
})
```

### 示例2：执行确认

```javascript
AskQuestion({
  questions: [{
    id: "confirm_execute",
    prompt: "准备执行以下操作：\n\n1. 修改config.json配置\n2. 重启服务\n3. 验证功能\n\n影响：服务将重启约30秒\n\n是否确认执行？",
    options: [
      { id: "yes", label: "确认执行" },
      { id: "no", label: "取消" },
      { id: "review", label: "我需要重新查看方案" }
    ]
  }]
})
```

### 示例3：超时重新确认

```javascript
AskQuestion({
  questions: [{
    id: "retry_confirm",
    prompt: "【重新确认】之前的提问超时\n\n原问题：是否删除backup.sql文件？\n文件大小：500MB\n最后修改：2年前\n\n请明确选择：",
    options: [
      { id: "delete", label: "确认删除" },
      { id: "keep", label: "保留文件" }
    ]
  }]
})
```

## 🚨 违规示例（学习用）

### 违规示例1：聊天列选项
```
❌ 严重错误：
"有以下几个方案：
A. 使用Redis缓存
B. 使用Memcached
C. 使用本地缓存
您选择哪个？"

✅ 正确做法：
[使用AskQuestion工具展示这3个选项]
```

### 违规示例2：是非问句
```
❌ 错误：
"是否要执行这个操作？(是/否)"

✅ 正确：
AskQuestion({
  questions: [{
    id: "confirm",
    prompt: "是否执行此操作？",
    options: [
      { id: "yes", label: "是" },
      { id: "no", label: "否" }
    ]
  }]
})
```

### 违规示例3：自作主张选择
```
❌ 错误：
"有方案A和方案B，我觉得方案A更好，我采用方案A"

✅ 正确：
"发现方案A和方案B，建议：
- 方案A：优点是... 缺点是...
- 方案B：优点是... 缺点是..."
[使用AskQuestion让用户选择]
```

## 📝 自检清单（MANDATORY）

在任何可能需要用户输入的场景，必须检查：

```
=== AskQuestion使用自检 ===

【场景识别】
- [ ] 是否有2个以上选项/方案？
- [ ] 是否需要用户确认执行？
- [ ] 是否不确定用户偏好？
- [ ] 是否有歧义需要澄清？
- [ ] 是否有风险需要用户评估？
- [ ] 是否AskQuestion超时需要重试？

【禁止行为检查】
- [ ] 我没有在聊天中列出选项
- [ ] 我没有用"A. B. C."或"1. 2. 3."
- [ ] 我没有用"是/否"这种文字问句
- [ ] 我没有自作主张选择方案

【工具使用】
- [ ] 我准备使用AskQuestion工具
- [ ] 我准备了清晰的prompt
- [ ] 我准备了明确的options
- [ ] 我准备等待用户响应

=== 检查完毕，使用AskQuestion ===
```

## 🎯 核心要求总结

### 1. 全覆盖原则
**所有**涉及用户选择、判断、提问、确认的场景 → **必须**使用AskQuestion

### 2. 零例外原则
**没有例外** → 即使"显而易见"的选择也要AskQuestion

### 3. 格式唯一原则
**只有一种合法方式** → 使用AskQuestion工具，其他都违规

### 4. 严格禁止原则
**绝对禁止**在聊天中以任何形式列出选项

---

**记住：看到选择就想到AskQuestion。看到问句就想到AskQuestion。看到不确定就想到AskQuestion。**

**核心口诀：有选择必Ask，有确认必Ask，有不确定必Ask。聊天列选项，严重违规行为。**

