---
description: 决策变更与路径切换必须先征询用户同意
globs:
alwaysApply: true
---

# 决策变更审批规则（全局强制）

## 核心原则

**任何涉及「决策变更」或「路径切换」的行为，必须先切换到 Plan 模式进行正式提案，征得用户明确同意后，方可切回 Agent 模式执行。未经确认不得擅自变更。**

## 触发场景

以下场景必须先暂停执行，向用户说明情况并等待确认：

### 1. 决策变更
- 修改已确认的技术方案或架构设计
- 调整已商定的实现优先级或执行顺序
- 变更已选定的工具、框架或技术栈
- 推翻或修改之前分析得出的结论
- 改变已约定的数据模型、表结构或字段定义

### 2. 路径切换
- 原定方案遇到阻碍，需要改用替代方案实现
- 发现更优路径，想要替换当前实施路径
- 因外部条件变化（如服务不可用、数据不一致）需要调整执行方式
- 需要跳过某个步骤或合并/拆分任务
- 将同步操作改为异步，或反之

## 执行规范

### 询问方式（强制）：
1. 必须先调用 `SwitchMode` 切换到 **Plan 模式**
2. 在 Plan 模式下向用户完整陈述变更提案
3. 等待用户在 Plan 模式中确认或否决
4. 用户确认后，再切换回 Agent 模式执行变更
5. 禁止在 Agent 模式中直接询问并立即执行

### 询问时必须包含：
1. **当前状况**：简要说明正在做什么、遇到了什么
2. **变更内容**：明确说明要从 A 改为 B
3. **变更原因**：为什么需要变更
4. **影响评估**：变更会影响哪些已有内容或后续计划
5. **可选方案**：如果有多个替代方案，逐一列出优劣

### 禁止行为：
- 禁止未经询问直接切换实现路径
- 禁止在用户不知情的情况下修改已确认的方案
- 禁止以"优化"为名义擅自变更已有设计
- 禁止假设用户会同意而跳过确认步骤

### 例外情况（无需询问）：
- 纯粹的代码格式修正（不改变逻辑）
- 修复明显的语法错误或拼写错误
- 按用户已明确要求执行的操作
