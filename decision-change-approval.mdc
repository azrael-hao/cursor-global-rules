---
description: 决策变更与路径切换必须先征询用户同意
globs:
alwaysApply: true
---

# 决策变更审批规则

## 核心原则（双重标准）

### 标准1：任务的自然延续（最高优先级）

**任务的自然延续 → 必须直接执行，禁止询问**

**任务自然延续的定义**：
- 验证→修复、分析→实现、测试→修复→重测、发现问题→解决
- 关键特征：后续步骤是前一步的目的所在，用户请求时已隐含完整流程

**例外（需询问）**：多种方案选择、高风险不可逆操作、涉及业务决策

### 标准2：确定性原则

**能确认无误的 → 直接执行 | 不确定的 → 使用AskQuestion询问**

**确认无误的条件（需全部满足）**：
1. 任务明确（用户要求明确或是任务自然延续）
2. 技术确定（只有一种正确做法）
3. 影响可控（修改范围明确，无连锁影响）
4. 风险可接受（操作可逆或低风险）

## 完整判断流程

```
第1步：是任务的自然延续吗？
  ├─ 是 → 第2步：有例外情况吗（多方案/高风险/业务决策）？
  │         ├─ 有 → 使用AskQuestion
  │         └─ 无 → 直接执行
  └─ 否 → 第3步：满足全部4个确定性条件吗？
            ├─ 是 → 直接执行
            └─ 否 → 使用AskQuestion
```

## 必须使用AskQuestion的场景

1. **路径选择**：多种技术方案需权衡、不同修复策略各有利弊、架构设计多个方向
2. **高风险操作**：删除数据、force push、DROP TABLE、批量修改、生产环境操作
3. **业务决策**：优先级排序、成本权衡、功能取舍
4. **需求解释**：需求有多种理解、缺少关键信息、技术实现细节不明确

## 必须直接执行的场景

1. **任务自然延续（无例外）**：验证发现错误→直接修复、分析完成→直接实现、测试失败→直接修复重测
2. **明确的确定性任务**：用户明确要求"修复XX bug"、标准操作"格式化代码"、明确修改"更新依赖到X.X.X"

## 使用AskQuestion的正确方式

```javascript
AskQuestion({
  title: "简洁标题",
  questions: [{
    id: "q1",
    prompt: "清晰的问题描述？",
    options: [
      { id: "opt1", label: "选项1：描述+利弊" },
      { id: "opt2", label: "选项2：描述+利弊" }
    ],
    allow_multiple: false  // 单选false，多选true
  }]
})
```

## 自检清单

- [ ] 这是任务的自然延续吗？
- [ ] 如果是，是否存在例外（多方案/高风险/业务决策）？
- [ ] 如果不是，是否满足4个确定性条件？
- [ ] 我是否应该使用AskQuestion？

---

**记住：任务自然延续直接执行，不确定的必须询问。**
