---
description: 决策变更与路径切换必须先征询用户同意
globs:
alwaysApply: true
---

# 决策变更审批规则（全局强制）

## 核心原则（双重标准）

### 标准 1：任务的自然延续（最高优先级）

**任务的自然延续 → 必须直接执行，禁止询问**

#### 什么是"任务的自然延续"？

任务执行过程中的必然下一步，无需中断询问：

- **验证 → 修复**：验证发现错误 → 直接修复错误
- **分析 → 实现**：分析出解决方案 → 直接实现方案
- **发现问题 → 解决**：执行中发现依赖错误 → 直接修复依赖
- **测试 → 修复 → 重测**：测试失败 → 修复 → 再次测试

**关键特征**：
- 后续步骤是前一步骤的目的所在
- 用户请求任务时已经隐含了完整流程
- 中断询问会破坏任务完整性

**禁止行为**：
- ❌ "验证完成，发现 7 个错误，是否修复？"（必须直接修复）
- ❌ "分析完成，找到解决方案，是否实现？"（必须直接实现）
- ❌ "测试失败，是否修复？"（必须直接修复）

**重要例外**：
即使是任务的自然延续，如果存在以下情况，仍需使用 AskQuestion：
- **多种方案选择**：有多个可行方案需要权衡（如 3 种数据库修复策略）
- **高风险操作**：不可逆操作需要明确确认（如 force push）
- **业务决策**：涉及优先级、成本等业务判断

---

### 标准 2：确定性原则

**能确认无误的 → 直接执行**  
**不确定的 → 使用 AskQuestion 工具询问**

#### 什么是"能确认无误"？

满足以下**所有**条件即为"确认无误"：

1. **任务明确性**: 用户要求明确，或是任务的自然延续
2. **技术确定性**: 只有一种正确做法，没有多个方案需要选择
3. **影响可控性**: 修改范围明确，不会产生连锁影响
4. **风险可接受**: 操作可逆或低风险

---

### 两个标准的关系（完整判断逻辑）

#### 第 1 步：检查是否为任务的自然延续

**是任务的自然延续？**
- ✅ 是 → 进入第 2 步（检查例外情况）
- ❌ 否 → 跳到第 3 步（4 问检查）

#### 第 2 步：检查自然延续的例外情况

即使是任务的自然延续，是否存在以下情况？
- **多种方案需选择**？（如 3 种数据库修复策略）
- **高风险不可逆操作**？（如 force push、DDL）
- **涉及业务决策**？（如优先级、成本权衡）

**判断**：
- 有任何例外 → **使用 AskQuestion**
- 无例外 → **直接执行**

#### 第 3 步：4 问确定性检查（非自然延续时）

1. 任务明确？
2. 技术确定？
3. 影响清楚？
4. 可逆？

**判断**：
- 全部 ✅ → **直接执行**
- 任何 ❌ → **使用 AskQuestion**

---

**简化记忆**：
```
自然延续 + 无例外 = 直接执行（最快路径）
自然延续 + 有例外 = 询问用户
非自然延续 + 4问全过 = 直接执行
非自然延续 + 有疑问 = 询问用户
```

### 什么情况必须使用 AskQuestion？

**只要有任何一项不确定，立即使用 AskQuestion 询问：**

1. **方案不确定**: 存在多种实现方案，需要权衡选择
2. **影响范围不确定**: 不清楚会影响多少文件或系统
3. **用户意图不明确**: 不确定用户是否希望这样做
4. **存在风险**: 操作不可逆或可能产生副作用（如 force push、数据库 DDL）
5. **决策变更**: 要改变已确认的技术方案或实现路径

### AskQuestion 的正确用途

- ✅ **方案选择**: "发现两种修复方案，A 更快但精度略低，B 更精确但复杂，选择哪个？"
- ✅ **范围确认**: "发现还需要修改 8 个相关文件，是否一并修复？"
- ✅ **风险确认**: "需要执行 git force push 清理错误提交，是否继续？"
- ❌ **错误用法**: "验证完成，发现 7 个错误，是否修复？"（应直接修复）

## 常见不确定场景（需要使用 AskQuestion）

基于"4 问检查"，以下是典型的不确定场景：

### 1. 方案不确定（第 2 问 ❌）
- 有多种技术方案可选（如优化策略、架构设计）
- 不同方案有不同的优劣取舍
- 需要根据业务场景权衡选择

### 2. 范围不确定（第 3 问 ❌）
- 发现需要修改的文件比预期多
- 任务范围超出用户原始要求
- 需要额外的工作才能完成任务

### 3. 意图不明确（第 1 问 ❌）
- 用户没有明确说要做某事，是我主动发现的
- 不确定用户是否希望这样做
- 可能存在其他优先级考虑

### 4. 高风险操作（第 4 问 ❌）
- 不可逆操作（如 git force push、数据库 DDL）
- 可能产生副作用的修改
- 影响生产环境的操作

## 执行规范

### 决策流程（双重标准）

```
开始任务
   ↓
检查：任务的自然延续？────────┐
   ↓                        │
  是                       否
   ↓                        ↓
直接执行              检查：4 问全过？
   ↓                   ↓         ↓
完成                 是        否
                      ↓         ↓
                  直接执行   AskQuestion
                      ↓         ↓
                    完成    等待用户选择
                               ↓
                          执行用户选择
```

**核心理念**：
- **第一优先级**：任务的自然延续 → 直接执行（体现任务完整性）
- **第二优先级**：确定性检查 → 全确定则执行，有疑问则询问（体现谨慎性）

### 使用 AskQuestion 的标准流程

当遇到不确定情况时：

1. **不需要切换 Plan 模式**（可在 Agent 模式直接使用 AskQuestion）
2. 使用 `AskQuestion` 工具向用户呈现选项
3. 提供清晰的选择项（通常 2-4 个选项）
4. 等待用户点击选择
5. 根据用户选择继续执行

**重要**：
- 使用 `AskQuestion` 工具，不要让用户手动输入回答
- 提供明确的选项让用户点击
- 不会打断聊天流程（问题和选项以交互式方式呈现）

### 询问时必须包含

使用 AskQuestion 时，在 `prompt` 中简要说明：
1. **当前状况**: 正在做什么、遇到了什么
2. **不确定的点**: 为什么需要询问
3. **可选方案**: 在 options 中列出 2-4 个明确的选项

## 执行前快速检查（优先级顺序）

在开始任何工具调用前，按以下顺序检查：

### 第 0 问（最高优先级）：这是任务的自然延续吗？

- ✅ **是** → **直接执行，不再检查后续问题**
  - 验证 → 修复
  - 分析 → 实现
  - 发现问题 → 解决
  - 测试 → 修复 → 重测
  
- ❌ **否** → 继续检查下面 4 个问题

---

### 如果不是自然延续，检查以下 4 个问题：

#### 1. 任务明确吗？
- ✅ 用户明确要求 → 继续检查
- ❌ 用户没说要这么做，我在主动扩展 → **使用 AskQuestion**

#### 2. 技术路径确定吗？
- ✅ 只有一种正确做法 → 继续检查
- ❌ 有多种方案需要选择 → **使用 AskQuestion**

#### 3. 影响范围清楚吗？
- ✅ 知道会改几个文件，改什么内容 → 继续检查
- ❌ 不确定会影响多少地方 → **使用 AskQuestion**

#### 4. 操作可逆吗？
- ✅ 可以撤销或低风险 → **直接执行**
- ❌ 不可逆或高风险（force push、DDL）→ **使用 AskQuestion**

---

### 判断流程图

```
开始任务
   ↓
第 0 问：任务的自然延续？
   ↓           ↓
  是          否
   ↓           ↓
直接执行    检查 4 个问题
   ↓           ↓
完成      全部✅?  有❌?
            ↓       ↓
         直接执行  AskQuestion
```

**记忆口诀**：
- **自然延续 = 直接干**
- **4 问全过 = 直接干**
- **任何存疑 = 先问问**

## 实际案例参考（基于双重标准）

### ✅ 案例 1：验证后修复错误（任务自然延续 - 直接执行）

**场景**: 用户要求"验证方案正确性"，发现 7 处 P0 错误，单文件修复

**第 0 问检查**: 
- 任务的自然延续？**✅ 是**
- 验证 → 修复 是完整任务流程
- 验证的目的就是发现并修复错误

**结论**: **直接执行**（无需检查 4 问，禁止询问）

**理由**: 这是任务的自然延续，中断询问"是否修复"会破坏任务完整性

---

### ❌ 案例 2：业务说明同步修改（使用 AskQuestion）

**场景**: 修复 ETL SQL 后，发现 8 个业务说明文档也需要同步更新

**第 0 问检查**: 
- 任务的自然延续？**❌ 否**
- 用户只要求"修复 SQL"，没说要改文档
- 这是发现的额外工作，不是自然延续

**继续 4 问检查**:
1. 任务明确？❌ 用户没说要改文档
2. 技术确定？✅ 改法明确
3. 影响范围？❌ 8 个文件，范围扩大
4. 可逆性？✅ Git 可回滚

**结论**: **使用 AskQuestion**（有 ❌，需要询问）

```python
AskQuestion(
    questions=[{
        "id": "sync_docs",
        "prompt": "修复 ETL SQL 后，发现 8 个业务说明文档引用了错误关联路径。是否同步修改这些文档？",
        "options": [
            {"id": "yes", "label": "是，一并修复保持一致"},
            {"id": "no", "label": "否，只修复 SQL"},
            {"id": "later", "label": "稍后单独处理"}
        ]
    }]
)
```

---

### ✅ 案例 3：用户明确纠正（确定性检查通过 - 直接执行）

**场景**: 用户说"只需要真实的文件名，不需要'文件名'这三个字"

**第 0 问检查**: 
- 任务的自然延续？❌ 否（是用户的新纠正指令）

**继续 4 问检查**:
1. 任务明确？✅ 用户明确纠正
2. 技术确定？✅ 删除指定文字
3. 影响范围？✅ 29 个文件格式统一修改
4. 可逆性？✅ Git 可回滚

**结论**: **直接执行**（4 问全 ✅）

---

### ❌ 案例 4：发现多种修复方案（使用 AskQuestion）

**场景**: 数据库连接失败，可以 1) 重试 2) 使用缓存 3) 降级查询

**第 0 问检查**: 
- 任务的自然延续？✅ 是（连接失败 → 修复连接）

**但技术路径不确定**:
- 有 3 种修复方案，需要权衡
- 虽然是自然延续，但方案选择需要用户决策

**结论**: **使用 AskQuestion**

**特殊说明**: 即使是任务的自然延续，如果存在多种方案需要选择，也应使用 AskQuestion 让用户决策

```python
AskQuestion(
    questions=[{
        "id": "db_strategy",
        "prompt": "数据库连接失败，建议选择恢复策略：",
        "options": [
            {"id": "retry", "label": "自动重试（可能延迟）"},
            {"id": "cache", "label": "使用缓存数据（可能过期）"},
            {"id": "degrade", "label": "降级查询（功能受限）"}
        ]
    }]
)
```

---

### ❌ 案例 5：高风险操作（使用 AskQuestion）

**场景**: 需要执行 `git push --force` 清理错误提交

**第 0 问检查**: 
- 任务的自然延续？✅ 是（发现错误提交 → 清理提交）

**但风险不可接受**:
- 操作不可逆，会永久改变远程仓库历史
- 虽然是自然延续，但高风险需要明确确认

**结论**: **使用 AskQuestion**

**特殊说明**: 任务的自然延续 + 高风险操作 = 仍需询问确认

```python
AskQuestion(
    questions=[{
        "id": "force_push",
        "prompt": "需要执行 git push --force 清理远程错误提交。此操作不可逆，是否继续？",
        "options": [
            {"id": "yes", "label": "是，执行 force push"},
            {"id": "no", "label": "否，保持原样"}
        ]
    }]
)
```

## 使用 AskQuestion 工具的标准模板

当遇到不确定情况时（4 问检查有任何 ❌），使用以下格式：

### 标准格式

```python
AskQuestion(
    questions=[{
        "id": "unique_id",  # 使用描述性 ID
        "prompt": "[简要说明情况] + [询问的问题]",
        "options": [
            {"id": "option1", "label": "选项 1 描述"},
            {"id": "option2", "label": "选项 2 描述"},
            {"id": "option3", "label": "选项 3（可选）"}
        ]
    }]
)
```

### 模板示例

#### 方案选择型
```python
AskQuestion(
    questions=[{
        "id": "solution_choice",
        "prompt": "发现 [问题]，有以下 [N] 种解决方案，请选择：",
        "options": [
            {"id": "solution_a", "label": "方案 A：[简述] - 优点：[X] 缺点：[Y]"},
            {"id": "solution_b", "label": "方案 B：[简述] - 优点：[X] 缺点：[Y]"}
        ]
    }]
)
```

#### 范围确认型
```python
AskQuestion(
    questions=[{
        "id": "scope_confirm",
        "prompt": "完成 [任务 A] 后，发现还需要 [任务 B]（涉及 [N] 个文件）。是否继续？",
        "options": [
            {"id": "continue", "label": "是，继续处理"},
            {"id": "skip", "label": "否，只完成当前任务"},
            {"id": "review", "label": "先查看影响范围"}
        ]
    }]
)
```

#### 风险确认型
```python
AskQuestion(
    questions=[{
        "id": "risk_confirm",
        "prompt": "需要执行 [操作]。此操作 [风险描述]，是否继续？",
        "options": [
            {"id": "proceed", "label": "是，理解风险并继续"},
            {"id": "cancel", "label": "否，取消操作"}
        ]
    }]
)
```

### 要点

1. **prompt 要简洁**: 1-2 句话说清楚情况和问题
2. **options 要明确**: 每个选项都应该是可直接执行的决策
3. **通常 2-4 个选项**: 太多会让用户困惑
4. **不需要 title**: 单个问题时 title 可省略
