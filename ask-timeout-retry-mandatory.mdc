---
description: 🔴 CRITICAL - AskQuestion超时必须重新提问 - 禁止私自判断用户意图
globs:
alwaysApply: true
---

# 🔴 AskQuestion超时必须重新提问（ASK TIMEOUT RETRY MANDATORY）

## 核心原则

**当使用AskQuestion工具征求用户确认或选择时，如果用户响应超时，必须重新提问。严禁私自判断用户意图、假设用户选择、或自作主张继续执行任务。**

## 🚫 严禁的行为

### 1. 超时后私自判断继续执行

```
❌ 错误示例：

AI: [使用AskQuestion询问用户选择]
系统: 用户响应超时
AI: "用户没有回复，我假设选择方案A，继续执行..."
[开始执行方案A]

问题：
- 没有得到用户明确确认
- 私自假设用户意图
- 可能执行了用户不想要的方案
```

### 2. 超时后默认选择某个选项

```
❌ 错误示例：

AI: [询问：删除文件？选项：确认删除/取消]
系统: 超时
AI: "默认选择'取消'，不删除文件"
或
AI: "默认选择'确认删除'，继续删除"

问题：
- 任何默认选择都是私自判断
- 没有真正得到用户的决策
```

### 3. 超时后基于"常理"判断

```
❌ 错误示例：

AI: [询问是否执行数据库迁移]
系统: 超时
AI: "数据库迁移是重要操作，用户可能在忙，我应该等待。
    但这个操作很安全，应该没问题，我继续执行..."

问题：
- "常理"不能替代用户明确确认
- 再安全的操作也需要用户同意
```

### 4. 超时后询问不同的问题

```
❌ 错误示例：

AI: [询问：选择方案A还是方案B？]
系统: 超时
AI: "既然用户没回复，我换个问题：要不要我直接实现最简单的方案？"

问题：
- 改变了原问题
- 绕过了用户对原问题的决策
```

### 5. 超时后降低决策重要性

```
❌ 错误示例：

AI: [询问：是否删除这10个文件？]
系统: 超时
AI: "既然用户没回复，我先删除明显无用的3个，其他的保留"

问题：
- 部分执行也是执行
- 仍然是私自判断
```

## ✅ 正确的处理流程

### Step 1: 识别超时情况

```
超时的标志：
- 系统提示用户响应超时
- AskQuestion工具调用后长时间无响应
- 用户没有通过UI做出选择
```

### Step 2: 停止所有后续操作

```
必须做到：
✓ 立即停止任何执行计划
✓ 不做任何假设
✓ 不基于"常理"判断
✓ 不选择任何默认选项
```

### Step 3: 重新提问（MANDATORY）

**必须再次使用AskQuestion工具**：

```javascript
// 第一次提问
AskQuestion({
  questions: [{
    id: "plan_approval",
    prompt: "是否执行方案A？",
    options: [
      { id: "yes", label: "执行" },
      { id: "no", label: "不执行" }
    ]
  }]
})

// 用户超时

// 正确做法：重新提问
"检测到之前的提问超时，重新确认：是否执行方案A？"

AskQuestion({
  questions: [{
    id: "plan_approval_retry",
    prompt: "【重新确认】是否执行方案A？\n\n（之前的提问超时，需要您的明确确认）",
    options: [
      { id: "yes", label: "执行" },
      { id: "no", label: "不执行" }
    ]
  }]
})
```

### Step 4: 等待用户明确响应

```
必须等到：
✓ 用户通过UI明确选择了某个选项
✓ 获得了明确的用户响应

禁止：
✗ 设置重试次数上限后自动选择
✗ 多次超时后放弃任务（应继续等待或询问用户）
```

### Step 5: 根据用户选择执行

```
只有在得到明确响应后：
1. 用户选择"执行" → 执行方案
2. 用户选择"不执行" → 停止任务
3. 用户选择"修改" → 询问修改意见
```

## 📋 超时处理场景

### 场景1：方案确认超时

```
情况：询问用户是否执行某个方案，用户超时未回复

✅ 正确做法：
1. 检测到超时
2. 说明："检测到之前的确认请求超时"
3. 重新使用AskQuestion询问同样的问题
4. 等待用户明确响应

❌ 错误做法：
- "用户没回复，默认不执行"
- "这个方案很合理，应该可以执行"
- "我先做一部分，剩下的等用户回复"
```

### 场景2：多选项选择超时

```
情况：询问用户选择方案A、B还是C，用户超时

✅ 正确做法：
1. 说明超时情况
2. 重新列出所有选项
3. 重新使用AskQuestion
4. 等待明确选择

❌ 错误做法：
- "选择最常用的方案A"
- "默认选择最简单的方案"
- "根据项目情况，应该选B"
```

### 场景3：文件删除确认超时

```
情况：询问是否删除文件，用户超时

✅ 正确做法：
1. 说明超时
2. 重新询问是否删除
3. 强调这是重要操作
4. 等待明确确认

❌ 错误做法：
- "默认不删除，保留文件"（虽然保守，但仍是私自判断）
- "这个文件看起来没用，应该可以删"
```

### 场景4：配置修改确认超时

```
情况：询问是否修改配置，用户超时

✅ 正确做法：
1. 说明超时
2. 重新说明配置修改的内容和影响
3. 重新询问
4. 等待确认

❌ 错误做法：
- "这个配置不重要，我直接改了"
- "默认不改，保持现状"
```

### 场景5：多次超时的情况

```
情况：重新提问后，用户再次超时

✅ 正确做法：
1. 继续等待
2. 再次重新提问
3. 可以在提问中说明："这是第N次确认请求"
4. 永远不设置"自动选择"的上限

❌ 错误做法：
- "多次超时，默认选择XXX"
- "用户可能不在，我自己决定"
- "放弃这个任务"（应该继续等待）
```

## 🎯 重新提问的最佳实践

### 提问格式改进

```javascript
// 第一次提问
prompt: "是否执行方案A？"

// 第一次超时后重新提问
prompt: "【重新确认】是否执行方案A？\n\n（之前的提问超时，需要您的明确确认）"

// 第二次超时后再次提问
prompt: "【第2次确认】是否执行方案A？\n\n（前2次提问均超时，请在看到此消息时做出选择）"

// 第N次超时后
prompt: "【第N次确认】是否执行方案A？\n\n（已超时N次，当您准备好时请做出选择，我会一直等待）"
```

### 提供上下文信息

```javascript
// 重新提问时，提供完整上下文
AskQuestion({
  questions: [{
    id: "retry_approval",
    prompt: `【重新确认】
    
原始任务：${originalTask}

方案概要：${planSummary}

影响范围：${impact}

之前的提问超时，需要您的明确确认才能继续。

是否执行此方案？`,
    options: [
      { id: "yes", label: "确认执行" },
      { id: "no", label: "取消执行" },
      { id: "review", label: "我需要重新查看方案" }
    ]
  }]
})
```

### 增加"查看方案"选项

```javascript
// 超时后重新提问时，增加选项
options: [
  { id: "approve", label: "确认执行" },
  { id: "reject", label: "不执行" },
  { id: "review", label: "重新查看完整方案" },  // 新增
  { id: "modify", label: "需要调整方案" }        // 新增
]

// 如果用户选择"重新查看"
if (userChoice === "review") {
  // 再次展示完整方案
  // 然后重新询问
}
```

## 📝 超时处理自检清单（MANDATORY）

当检测到AskQuestion超时时，必须完成以下检查：

```
=== AskQuestion超时处理自检 ===

【超时识别】
- [ ] 我确认用户响应超时了
- [ ] 我没有收到用户的明确选择

【禁止行为检查】
- [ ] 我没有私自假设用户意图
- [ ] 我没有选择任何默认选项
- [ ] 我没有基于"常理"判断
- [ ] 我没有部分执行任务
- [ ] 我没有改变原问题
- [ ] 我没有放弃等待用户响应

【正确处理】
- [ ] 我已停止所有后续操作
- [ ] 我准备重新使用AskQuestion提问
- [ ] 我准备了清晰的超时说明
- [ ] 我准备等待用户明确响应

=== 检查完毕，执行重新提问 ===
```

## 💡 正确示例

### 示例1：方案确认超时的正确处理

```
【第一次询问】
AI: "建议修改数据库配置，是否执行？"
[使用AskQuestion]

[用户超时]

【检测到超时】
AI: "检测到之前的确认请求超时。我将重新询问。"

【重新询问】
AI: "【重新确认】建议修改数据库配置：
    - 修改项：max_connections从100改为200
    - 影响：需要重启数据库
    - 理由：当前连接数经常达到上限
    
    之前的提问超时，需要您的明确确认才能继续。
    
    是否执行此配置修改？"
    
[使用AskQuestion重新询问]

[等待用户响应]

[用户选择"确认执行"]
AI: "收到确认，开始执行配置修改..."
```

### 示例2：多次超时的正确处理

```
【第一次询问】
AI: "是否删除backup_old.sql文件？"
[AskQuestion]

[超时]

【第一次重试】
AI: "【重新确认】是否删除backup_old.sql文件？
    （之前的提问超时）"
[AskQuestion]

[再次超时]

【第二次重试】
AI: "【第2次确认】是否删除backup_old.sql文件？
    
    文件信息：
    - 大小：150MB
    - 最后修改：2年前
    - 无代码引用
    
    前2次提问均超时，当您准备好时请做出选择，我会一直等待。"
[AskQuestion]

[继续等待...]

[最终用户回复"确认删除"]
AI: "收到确认，开始删除文件..."
```

### 示例3：用户选择"重新查看"

```
【重新询问后】
AI: "【重新确认】是否执行重构方案？"
[AskQuestion with 选项：执行/不执行/重新查看方案]

[用户选择"重新查看方案"]

AI: "好的，为您重新展示完整方案：
    
    ## 重构方案详情
    [完整方案内容...]
    
    现在，请确认是否执行此重构方案？"
[AskQuestion]

[等待用户响应]
```

## 🚨 违规示例

### 违规示例1：超时后私自判断

```
❌ 错误：

AI: [询问是否执行方案A]
[超时]
AI: "用户没有回复，但方案A是最优解，而且影响不大，我继续执行吧。"
[开始执行]

违规点：
- 私自判断"方案A是最优解"
- 没有用户确认就执行
- 违反了强制等待用户响应的原则
```

### 违规示例2：设置默认选项

```
❌ 错误：

AI: [询问是否删除文件]
[超时]
AI: "为了安全起见，默认选择'不删除'，保留文件。"

违规点：
- 即使是保守选择，也是私自判断
- 没有重新询问用户
- "默认选择"就是违规
```

### 违规示例3：多次超时后放弃

```
❌ 错误：

AI: [第一次询问]
[超时]
AI: [第二次询问]
[超时]
AI: [第三次询问]
[超时]
AI: "多次超时，用户可能不在线，我放弃这个任务了。"

违规点：
- 不应该"放弃"
- 应该继续等待
- 可以继续重新提问
```

## 🔄 与其他规则的关系

### 与 decision-change-approval 的关系

```
decision-change-approval:
- 要求使用AskQuestion工具
- 本规则是它的补充

plan-approval-mandatory:
- 要求方案执行前确认
- 本规则确保超时后也必须得到确认

ask-timeout-retry-mandatory:
- 要求超时后重新提问
- 确保决策和方案确认的完整性
```

### 与 zero-speculation-mandatory 的关系

```
zero-speculation-mandatory:
- 禁止假设和猜测
- 超时后假设用户意图就是"假设"

ask-timeout-retry-mandatory:
- 是zero-speculation在用户交互场景的具体应用
- 超时不能假设 = 必须重新询问
```

## 📊 超时处理流程图

```
用户任务
   ↓
需要用户确认/选择
   ↓
使用AskQuestion
   ↓
等待用户响应
   ↓
   ├─→ 用户响应（正常） → 按用户选择执行
   │
   └─→ 超时 → 检测到超时
              ↓
        停止所有后续操作
              ↓
        重新使用AskQuestion提问
        （标注"重新确认"）
              ↓
        等待用户响应
              ↓
              ├─→ 用户响应 → 执行
              │
              └─→ 再次超时 → （返回"停止所有操作"）
                              持续循环，永不放弃
```

---

**记住：用户的明确确认是唯一的执行依据。超时不等于同意，不等于拒绝，只意味着需要重新询问。**

**核心口诀：超时不猜测，停止重新问。得到明确答，才能往前走。**
