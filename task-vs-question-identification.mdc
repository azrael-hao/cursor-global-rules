---
description: 准确识别用户意图：区分需要执行的任务和只需回答的问题，避免过度执行或仅提供解释。
globs:
alwaysApply: true
---

# 任务与问题识别规则

## 核心原则

**准确判断用户意图，执行类任务用工具完成，回答类问题直接解释。**

## 识别流程

```
用户输入 → 分析意图特征
├─ 执行类任务 → 使用工具执行 → 简洁总结
└─ 回答类问题 → 直接回答（必要时查询）
```

## 执行类任务（需要使用工具）

### 特征识别
- **明确动作词**：创建、修改、删除、更新、添加、实现、开发、编写、生成、修复、优化、重构、部署、配置、安装、运行、执行、测试
- **隐含执行意图**："帮我XX"、"给我XX"（后接具体操作）、提供了具体修改内容、有文件/代码引用（@文件）+ 改进性描述

### 处理方式
1. 使用工具执行（Read、Write、StrReplace、Shell等）
2. 完成后简洁总结
3. 禁止只提供方案不执行（除非需要用户决策）

### 示例
"修改配置文件端口为8080" ✅ 执行 | "创建用户登录API" ✅ 执行 | "优化这段代码性能" ✅ 执行

## 回答类问题（只需要回答）

### 特征识别
- **疑问词**：是什么、为什么、怎么样、如何、有没有、能不能、会不会、什么时候、在哪里
- **信息查询意图**：询问原理、概念、定义、最佳实践；询问可能性、可行性、原因、解释；无明确执行动作要求

### 处理方式
1. 直接回答，提供清晰解释
2. 必要时用Read/Grep查询信息
3. 禁止主动修改或创建文件

### 示例
"这段代码什么意思？" ✅ 回答 | "为什么会出现这个错误？" ✅ 回答 | "有哪些优化方案？" ✅ 回答

## 模糊场景判断

### "如何XX"型问题
- 有具体代码/文件引用 → 倾向执行
- 无具体上下文 → 倾向回答方法

### "能不能XX"型问题  
- 有文件引用 + 任务背景 → 倾向执行
- 纯询问语气 → 倾向回答可行性

### 判断信号
- **倾向执行**：提供文件路径或@文件引用、有明确任务背景、语气坚定指令性强
- **倾向回答**：纯理论概念询问、无具体上下文或文件引用、探索性咨询性语气

## 错误行为避免

- ❌ 过度执行：用户问"这段代码有什么问题？" → 错：直接修改代码 | 正：分析问题说明原因
- ❌ 只说不做：用户说"修改配置文件端口为8080" → 错：只解释如何修改 | 正：直接修改简洁总结
- ❌ 误判意图：用户问"能不能优化这段代码？"（有@代码引用）→ 错：只回答方法 | 正：根据上下文执行或询问确认

## 自检清单

- [ ] 正确识别了执行类任务还是回答类问题？
- [ ] 执行类任务使用了工具实际执行？
- [ ] 回答类问题避免了不必要的文件修改？
- [ ] 模糊场景考虑了上下文和明确性？

**记住：任务要执行，问题要回答，不确定时先明确意图。**
