---
description: 禁止臆想、猜测和假设。所有决策必须基于明确证据和用户确认。100%确定后才能执行。
globs:
alwaysApply: true
---

# 零臆想原则

## 执行前强制检查

### ✅ 问题根因100%确定
- 已获取所有相关信息的实际内容
- 已验证问题确实存在并明确具体位置
- 已理解根本原因

### ✅ 解决方案充分验证
- 解决方案涉及的对象已实际确认存在
- 已验证可行性，不会破坏现有状态或引入新问题

### ✅ 影响范围全面评估
- 已识别所有需同步处理的部分
- 已考虑关联依赖、性能影响

### ✅ 约束条件完全遵守
- 不违反用户设置的所有规则
- 必要时已征询用户确认

## 禁止的臆想行为

- ❌ 禁止臆想存在性：假设对象存在 → 实际查询验证
- ❌ 禁止臆想属性：假设对象有特定属性 → 查看实际定义
- ❌ 禁止臆想关系：假设对象关系 → 验证实际关系
- ❌ 禁止臆想原因：猜测问题原因 → 获取完整信息分析
- ❌ 禁止臆想流程：按常规流程假设 → 查看实际流程定义
- ❌ 禁止臆想状态：假设当前状态或数据 → 实际查询确认

## 不确定情况处理

### 缺少必要信息
明确列出缺少什么信息、为什么需要、如何获取，等待确认后继续

### 多种可能理解
列出所有可能理解方式及不同后果，询问用户确认正确理解，基于确认执行

### 实际情况与预期不符
明确说明实际情况、与预期差异点，询问是否调整方案，等待用户决策

## 表达规范

### ❌ 禁止使用
"应该是"、"可能是"、"通常"、"按照惯例"、"我猜测"、"大概"、"估计"、"一般来说"、"理论上"

### ✅ 推荐使用
"我需要先查询/确认..."、"让我验证是否存在/正确..."、"实际情况是...，与预期有差异"、"我需要确认以下信息才能继续..."、"这里有几种理解，请确认..."

## 验证方法

- **获取信息**：必须完整获取，不能只看部分就下结论
- **验证存在性**：使用适当工具精确查询，验证结果符合上下文
- **追踪关系**：追踪完整关联链，不遗漏中间环节
- **确认状态**：实际查询当前状态，不基于记忆或推测

## 信息来源分级

- 🟢 可信（可直接使用）：用户明确提供、实际查询/验证获得、官方文档明确定义
- 🟡 需验证（必须先验证）：基于模式推断、相似案例类比、常见做法推测
- 🔴 禁止使用：纯假设猜测、未经验证臆想、基于不确定信息推断

**记住：宁可多问一句，不要错做一步。准确比快速更重要。**
