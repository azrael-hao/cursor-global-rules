---
description: 复杂任务实现前必须切换到Plan模式，提供方案和计划，避免直接编码导致返工
globs:
alwaysApply: true
---

# 计划优先原则

## 核心原则

**复杂任务必须先规划后实现。禁止在Agent模式下直接实现复杂功能。**

## 强制切换Plan模式的场景

### 🔴 必须切换（MANDATORY）

遇到以下任何一种情况，必须主动调用`SwitchMode`切换到Plan模式：

1. **架构设计决策**
   - 新功能涉及多个模块/文件
   - 需要选择技术方案（数据库设计、API设计、算法选择）
   - 涉及系统架构调整或重构

2. **多种实现路径**
   - 有2种以上实现方式
   - 需要权衡性能、复杂度、可维护性
   - 涉及技术选型（框架、库、工具）

3. **复杂业务逻辑**
   - 涉及3个或以上模块的交互
   - 需要修改5个或以上文件
   - 业务流程有多个分支或状态

4. **高风险操作**
   - 数据库schema变更
   - 批量数据迁移
   - 破坏性API变更
   - 影响生产环境的修改

5. **需求不明确**
   - 用户需求有歧义
   - 缺少关键实现细节
   - 需要补充业务规则

6. **大型重构**
   - 代码重构涉及多个文件
   - 性能优化需要系统性分析
   - 技术债务清理

### ✅ 可直接实现（无需Plan）

以下简单任务可在Agent模式直接执行：

- 单文件小修改（<20行）
- 明确的bug修复（根因已知）
- 简单的配置调整
- 文档更新
- 单元测试补充

## Plan模式的职责

切换到Plan模式后必须提供：

### 1. 问题分析
- 当前需求的核心问题
- 涉及的模块和边界
- 潜在风险和约束

### 2. 方案设计
- **多方案对比**：列出2-3种可行方案
- **方案评估**：
  - ✅ 优点
  - ❌ 缺点
  - ⚠️ 风险
  - 🎯 推荐理由

### 3. 实施计划
- 分步骤列出实施流程
- 标注关键文件和修改点
- 说明依赖关系
- 预估工作量

### 4. 决策确认
- 明确需要用户决策的点
- 使用`AskQuestion`工具获取确认
- 等待用户选择后再切换回Agent模式

## 工作流程

```
收到复杂任务
  ↓
【thinking检查】是否需要Plan模式？
  ↓
是 → SwitchMode(plan) → 提供方案和计划 → 用户确认 → SwitchMode(agent) → 实施
否 → 直接在Agent模式实施
```

## 切换示例

### ✅ 正确示例

```
用户："添加Redis缓存功能"

【thinking】
- 涉及多个模块（API层、数据层、配置层）
- 需要选择缓存策略（穿透、雪崩、一致性）
- 需要设计key命名规范
→ 需要切换Plan模式

【操作】
SwitchMode({
  target_mode_id: "plan",
  explanation: "缓存功能涉及架构设计和多种策略选择，需要先规划方案"
})
```

### ❌ 错误示例

```
用户："添加Redis缓存功能"

【错误操作】
直接开始修改代码，边写边想
→ 导致方案不合理、需要返工
```

## 自检清单

在thinking中必须检查：

- [ ] 这是复杂任务吗？（涉及多个文件/模块）
- [ ] 有多种实现方案吗？
- [ ] 涉及架构决策吗？
- [ ] 需求完全明确吗？
- [ ] 有高风险操作吗？

**任何一项为"是"→ 必须切换Plan模式**

## 特殊情况

### 任务的自然延续

如果已在Plan模式完成规划并获得用户确认，切换到Agent模式后可直接实施，无需再次规划。

### 紧急bug修复

如果是明确的紧急bug且根因已知，可以：
1. 简要说明修复方案
2. 直接实施
3. 事后补充完整分析（如需要）

## 违规后果

**违反本规则将导致：**
- ❌ 方案不合理需要返工
- ❌ 遗漏关键场景
- ❌ 技术债务累积
- ❌ 用户满意度下降

---

**记住：复杂任务必须先Plan后Code。宁可多花5分钟规划，不要浪费30分钟返工。**
