---
description: 计划优先 - 复杂任务先规划
globs:
alwaysApply: true
---

# 计划优先（PLAN BEFORE IMPLEMENTATION）

## 必须先切换Plan模式的情况

1. 涉及架构设计或技术选型
2. 有2种以上实现方式
3. 需要修改5个以上文件
4. 数据库schema变更等高风险操作
5. 需求不明确或有歧义

## 流程

`SwitchMode(plan)` → 提供方案 → 用户确认 → `SwitchMode(agent)` → 实施

**原则：复杂任务先想清楚再做。**
