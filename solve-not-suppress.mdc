---
description: 🔴 CRITICAL - 必须解决问题而不是干掉问题 - 禁止掩盖、删除、忽略错误
globs:
alwaysApply: true
---

# 🔴 解决问题而非干掉问题（SOLVE NOT SUPPRESS）

## 核心原则

**必须真正解决问题的根本原因，严禁通过删除、注释、忽略、掩盖等方式"干掉"问题。**

## 🚫 严禁的"干掉问题"行为

### 1. 删除代码（Delete）
```javascript
❌ 错误示例：
// 这个方法报错，直接删掉
// function validateUser(user) {
//   return user.email && user.password;
// }

✅ 正确做法：
// 找到报错原因，修复逻辑
function validateUser(user) {
  if (!user) return false;  // 处理null/undefined
  return user.email && user.password;
}
```

### 2. 捕获异常后不处理（Catch and Ignore）
```javascript
❌ 错误示例：
try {
  const data = await fetchData();
} catch (e) {
  // 吞掉异常，假装没发生
}

✅ 正确做法：
try {
  const data = await fetchData();
} catch (e) {
  logger.error('Failed to fetch data', e);
  // 根据业务决定：重试、降级、通知用户
  throw new ServiceException('Data fetch failed', e);
}
```

### 3. 注释掉失败的测试（Skip Tests）
```javascript
❌ 错误示例：
// test('should validate email', () => {
//   expect(validateEmail('test@example.com')).toBe(true);
// });

✅ 正确做法：
test('should validate email', () => {
  // 修复validateEmail函数的bug
  expect(validateEmail('test@example.com')).toBe(true);
});
```

### 4. 禁用警告/错误（Suppress Warnings）
```python
❌ 错误示例：
# pylint: disable=all
# 禁用所有检查来"解决"问题

✅ 正确做法：
# 修复实际的代码问题
# 仅在特殊情况下禁用特定规则，并说明原因
# pylint: disable=line-too-long  # API URL必须完整
```

### 5. 删除验证逻辑（Remove Validation）
```javascript
❌ 错误示例：
// 验证太严格导致失败，直接删除
function saveUser(user) {
  // if (!user.email) throw new Error('Email required');
  db.save(user);
}

✅ 正确做法：
function saveUser(user) {
  // 保留验证，修复调用方的数据问题
  if (!user.email) throw new Error('Email required');
  db.save(user);
}
```

### 6. 使用空实现（Empty Implementation）
```java
❌ 错误示例：
@Override
public void onError(Exception e) {
    // 空实现，忽略所有错误
}

✅ 正确做法：
@Override
public void onError(Exception e) {
    log.error("Error occurred", e);
    // 根据错误类型决定处理策略
    if (e instanceof RetryableException) {
        scheduleRetry();
    } else {
        notifyAdmin(e);
    }
}
```

### 7. 修改数据迁就代码（Change Data Instead of Code）
```sql
❌ 错误示例：
-- 代码假设status只能是1或2，但数据有3
-- 直接修改数据库把3改成2
UPDATE users SET status = 2 WHERE status = 3;

✅ 正确做法：
-- 修改代码支持status=3，或者理解为什么会有3
-- 如果3是错误数据，找出产生3的根因并修复
```

### 8. 降低标准（Lower Standards）
```javascript
❌ 错误示例：
// 密码验证总是失败，降低要求
function isValidPassword(pwd) {
  return pwd.length >= 3;  // 从8降到3
}

✅ 正确做法：
// 找出为什么验证失败，修复真正的问题
function isValidPassword(pwd) {
  return pwd.length >= 8 && /[A-Z]/.test(pwd) && /[0-9]/.test(pwd);
}
```

## ✅ 正确的问题解决流程

### Step 1: 定位根因（Root Cause Analysis）

```
问题：用户登录失败

❌ 干掉问题：注释掉密码验证
✅ 解决问题：
1. 查看错误日志：Password hash mismatch
2. 检查数据库：密码字段是bcrypt格式
3. 检查代码：验证用的是MD5
4. 根因：加密算法不匹配
5. 修复：统一使用bcrypt
```

### Step 2: 评估影响范围

```
修复前必须考虑：
- 这个修改会影响哪些功能？
- 需要同步修改哪些地方？
- 是否需要数据迁移？
- 是否向后兼容？
```

### Step 3: 实施修复

```
正确的修复包含：
1. 修复根本原因
2. 添加测试用例防止回归
3. 更新文档（如需要）
4. 验证修复效果
```

### Step 4: 验证解决

```
确认：
✓ 问题不再出现
✓ 没有引入新问题
✓ 相关测试通过
✓ 代码审查通过
```

## 📋 自检清单（MANDATORY）

在"解决"任何问题前，必须回答：

- [ ] 我是否真正理解了问题的根本原因？
- [ ] 我的修复是针对根因还是症状？
- [ ] 我是否在掩盖问题而不是解决问题？
- [ ] 删除/注释这段代码会导致什么后果？
- [ ] 忽略这个错误会带来什么风险？
- [ ] 我是否只是为了让代码"能跑"而不管正确性？
- [ ] 这个"修复"是否降低了系统的健壮性？

**任何一项答案不确定 → 不能"修复"，需要重新分析**

## 🎯 常见场景判断

### 场景1：测试失败

```
问题：新写的测试一直失败

❌ 干掉问题：
- test.skip('这个测试有问题')
- 删除测试
- 修改断言让测试通过

✅ 解决问题：
1. 分析为什么测试失败
2. 是代码bug？还是测试写错？
3. 修复实际的问题
4. 确保测试真正验证了业务逻辑
```

### 场景2：性能问题

```
问题：查询很慢

❌ 干掉问题：
- 删除这个查询功能
- 注释掉性能监控
- 提高超时时间

✅ 解决问题：
1. EXPLAIN分析查询计划
2. 添加必要的索引
3. 优化SQL语句
4. 验证性能提升
```

### 场景3：兼容性问题

```
问题：IE浏览器报错

❌ 干掉问题：
- 禁止IE访问
- 注释掉报错的功能

✅ 解决问题：
1. 分析具体是什么不兼容
2. 使用polyfill或替代方案
3. 降级处理（保持功能可用）
4. 文档说明支持的浏览器
```

### 场景4：数据不一致

```
问题：订单状态不对

❌ 干掉问题：
- 手动改数据库
- 删除校验逻辑

✅ 解决问题：
1. 追踪状态变更流程
2. 找到导致不一致的代码
3. 修复状态机逻辑
4. 添加事务保证一致性
5. 修复历史错误数据（一次性脚本）
```

### 场景5：异常报警

```
问题：生产环境一直报警

❌ 干掉问题：
- 关闭报警
- try-catch吞掉异常

✅ 解决问题：
1. 查看具体报警内容
2. 定位触发条件
3. 修复导致异常的根本原因
4. 如果是正常情况，调整报警规则
```

## 🚨 红线行为（NEVER DO）

以下行为属于严重违规：

1. **生产问题注释代码**
   - 绝不能在生产环境通过注释代码来"修复"
   
2. **静默失败**
   - 捕获异常后不记录、不通知、不处理

3. **删除业务逻辑**
   - 因为理解不了就删除关键业务代码

4. **跳过关键验证**
   - 为了方便测试跳过安全验证

5. **修改测试适配错误**
   - 代码错了，改测试让它通过

## 💡 何时可以"移除"

以下情况可以合理地删除/禁用：

### ✅ 合理的删除

```javascript
// 1. 废弃功能正式下线
// 已确认该功能不再使用，相关代码可以删除
function deprecatedFeature() { ... }  // 可以删除

// 2. 重复代码合并
// 两个实现相同功能的方法，保留一个
function calculateTotal() { ... }     // 保留
function computeTotal() { ... }       // 可以删除（重复）

// 3. 死代码清理
if (false) {  // 永远不会执行
  legacyCode();  // 可以删除
}
```

### ✅ 合理的忽略

```javascript
// 1. 明确的业务规则
try {
  await sendEmail(user);
} catch (e) {
  // 业务规则：发邮件失败不影响注册流程
  logger.warn('Email send failed, continuing', e);
  metrics.increment('email_send_failure');
}

// 2. 预期的异常
try {
  const cached = cache.get(key);
} catch (CacheMissException e) {
  // 缓存未命中是正常情况
  return fetchFromDb(key);
}
```

### ⚠️ 必须说明原因

任何"忽略"、"跳过"、"禁用"都必须：
1. 添加清晰的注释说明原因
2. 记录到文档或技术债务列表
3. 创建TODO或Issue跟踪
4. 定期review是否需要修复

```javascript
// FIXME: 临时忽略此验证，等待API v2上线 (Issue #1234)
// @deprecated 将在2026-06-01移除
// TODO: 迁移到新的认证方式后移除这个workaround
```

## 📝 修复记录模板

```markdown
## 问题描述
[具体什么问题]

## 根因分析
[经过调查，发现真正的原因是...]

## 修复方案
[针对根因的修复措施]

## 验证方式
[如何确认问题已解决]

## 影响范围
[这个修复影响了哪些模块]
```

---

**记住：优秀的工程师解决问题，糟糕的工程师掩盖问题。**

**核心口诀：找到根因，对症下药。不留隐患，不欠技术债。**
