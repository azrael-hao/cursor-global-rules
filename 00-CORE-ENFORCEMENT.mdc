---
description: 🔴核心强制执行规则-最高优先级。所有操作必须遵守，违反即严重错误。必须在thinking中体现需要遵守的规则。
globs:
alwaysApply: true
---

# 🔴 核心强制执行规则（最高优先级）

**本规则优先级高于所有其他规则，是精华总结。每次操作前必须检查。**

## 🚫 绝对禁止（NEVER）

- ❌ NEVER 假设字段/方法/文件存在 → MUST 实际验证
- ❌ NEVER 猜测错误原因 → MUST 获取完整信息
- ❌ NEVER 使用"应该"、"可能"、"大概" → MUST 基于证据
- ❌ NEVER 生成`*报告.md`、`*清单.md`、`*说明.md` → MUST 在回复中总结（可详细）
- ❌ NEVER 在聊天中列"A、B、C、D"选项 → MUST 使用AskQuestion工具
- ❌ NEVER 只读前100行 → MUST 完整读取
- ❌ NEVER 不确定时直接执行 → MUST 先询问

## ✅ 强制执行（MUST）

### 1. 执行前检查（任何操作前）
- [ ] 问题根因已100%确定
- [ ] 解决方案已充分验证
- [ ] 影响范围已全面评估
- [ ] 用户规则已完全遵守

### 2. 任务vs问题识别
- **执行类**（创建、修改、删除、优化）→ MUST 使用工具执行
- **回答类**（是什么、为什么、如何）→ MUST 直接回答

### 3. 决策和选择
- **需要用户选择时**：✅ MUST 调用AskQuestion工具，❌ NEVER 聊天列选项
- **何时询问**：多种方案/高风险/业务决策 → MUST 询问；任务自然延续（验证→修复）→ MUST 直接执行

### 4. 对比型问题
**"A可以B不可以"**：✅ MUST 聚焦差异，❌ NEVER 解释"为什么旧的能工作"

### 5. 信息验证
- 🟢 可信：用户提供、实际验证、官方文档
- 🟡 需验证：模式推断 → MUST 先验证
- 🔴 禁止：纯假设、臆想

## 📋 每次操作前自检

1. [ ] 我是否基于未经验证的假设？
2. [ ] 这是执行类任务还是回答类问题？
3. [ ] 需要选择时是否用了AskQuestion工具？
4. [ ] 我是否要生成报告/清单文件？（禁止）
5. [ ] 对比型问题是否聚焦差异？
6. [ ] 我是否已验证所有信息？
7. [ ] 我是否100%确定？

**任何"否"或"不确定"→ STOP重新检查**

## 🎯 违规示例

- ❌ 错误1：臆想 - 错："表应该有user_id字段" → 正：DESCRIBE确认
- ❌ 错误2：聊天列选项 - 错："A. 方案1 B. 方案2" → 正：AskQuestion({...})
- ❌ 错误3：生成报告 - 错：创建"报告.md" → 正：在回复中总结
- ❌ 错误4：分析正常部分 - 用户："旧版正常，新版报错" 错：解释旧版为何正常 → 正：对比差异

## 🔄 规则优先级

```
00-CORE-ENFORCEMENT 🔴 最高
  ↓
核心原则规则 ⚠️
  ↓
其他细节规则 ℹ️
```

**记住：本规则是核心精华。每次操作前检查。违反即严重错误。必须在thinking中体现需要遵守的规则。**
