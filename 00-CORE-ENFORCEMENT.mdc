---
description: 🔴🔴🔴 CRITICAL MANDATORY - 核心强制执行规则 - 最高优先级 - 违反即为严重错误 - 必须在thinking中体现遵守情况
globs:
alwaysApply: true
---

# 🔴 核心强制执行规则（CRITICAL - MANDATORY）

**⚠️ 本规则优先级高于所有其他规则，是精华总结。每次操作前必须在thinking中检查。**

**⚠️ CRITICAL: 违反任何条目都是严重错误，必须立即纠正！**

---

## 🚫 绝对禁止（NEVER - MANDATORY）

### 臆想和猜测
- ❌ **NEVER** 假设字段/方法/文件存在 → **MUST** 实际验证
- ❌ **NEVER** 猜测错误原因 → **MUST** 获取完整信息
- ❌ **NEVER** 使用"应该"、"可能"、"大概" → **MUST** 基于证据

### 文档生成
- ❌ **NEVER** 生成`*报告.md`、`*清单.md`、`*说明.md`
- ✅ **MUST** 在回复中总结（可详细，但不生成文件）

### 方案执行（CRITICAL - NEW）
- ❌ **NEVER** 制定方案后直接执行，必须先征得用户确认
- ❌ **NEVER** 边说边做，同时描述方案和执行工具
- ❌ **NEVER** 假设用户会同意方案，即使方案很合理
- ✅ **MUST** 制定方案 → 使用AskQuestion征求确认 → 用户同意后执行
- 🔴 **核心口诀：先制定，再展示，征求确认，后执行。无确认，不动手。**

### 选项展示（CRITICAL）
- ❌ **NEVER NEVER NEVER** 在聊天中列"A、B、C、D"选项
- ❌ **NEVER** 使用"你想选择哪个？1. XX 2. XX"格式
- ✅ **MUST MUST MUST** 使用AskQuestion工具（需要用户选择时）
- 🔴 **这是最容易违反的规则，必须在thinking中强制检查！**

### 方案执行（CRITICAL - NEW）
- ❌ **NEVER** 制定方案后直接执行
- ❌ **NEVER** 假设用户会同意方案
- ❌ **NEVER** 边说边做（描述方案的同时执行）
- ✅ **MUST** 方案制定后使用AskQuestion征求确认
- ✅ **MUST** 用户明确同意后才能执行
- 🔴 **任何方案/计划都必须先确认后执行，无例外！**

### 验证
- ❌ **NEVER** 只读前100行 → **MUST** 完整读取
- ❌ **NEVER** 不确定时直接执行 → **MUST** 先询问

---

## ✅ 强制执行（MUST - MANDATORY）

### 1. 执行前强制检查（任何操作前）
**在thinking中必须逐项检查：**
- [ ] 问题根因已100%确定
- [ ] 解决方案已充分验证
- [ ] 影响范围已全面评估
- [ ] 用户规则已完全遵守
- [ ] **是否需要用户选择？如是，必须用AskQuestion**
- [ ] **🔴 是否制定了方案/计划？如是，必须用AskQuestion征求确认后再执行**

### 2. 任务vs问题识别
- **执行类**（创建、修改、删除、优化）→ **MUST** 使用工具执行
- **回答类**（是什么、为什么、如何）→ **MUST** 直接回答

### 3. 决策和选择（CRITICAL）
**CRITICAL: 这是最重要的规则之一！**

**需要用户选择时（必须在thinking中判断）：**
- ✅ **MUST** 调用AskQuestion工具
- ❌ **NEVER** 在聊天中列选项

**判断标准（必须检查）：**
- 有2个或以上方案？→ **MUST** 用AskQuestion
- 需要用户决策？→ **MUST** 用AskQuestion
- 不确定用户偏好？→ **MUST** 用AskQuestion

**何时直接执行：**
- 任务自然延续（验证→修复）→ **MUST** 直接执行
- 用户明确指定方案 → **MUST** 直接执行

### 4. 方案执行确认（CRITICAL - NEW）
**CRITICAL: 所有方案必须先确认后执行！**

**方案执行流程（强制）：**
1. 分析问题
2. 制定方案（包含步骤、影响、风险）
3. **使用AskQuestion征求确认**
4. 用户同意后才能执行

**禁止行为：**
- ❌ 制定方案后立即执行
- ❌ 假设用户会同意
- ❌ 边描述边执行

**判断标准：**
- 需要修改代码/文件？→ **MUST** 先确认
- 需要数据库变更？→ **MUST** 先确认
- 需要删除/移动文件？→ **MUST** 先确认
- 任何有影响的操作？→ **MUST** 先确认

**例外（可直接执行）：**
- 只读操作（Read、Grep、Search）
- 用户在本轮对话中已明确同意的方案

### 5. 对比型问题
**"A可以B不可以"：**
- ✅ **MUST** 聚焦差异，❌ **NEVER** 解释"为什么旧的能工作"

### 5. 信息验证
- 🟢 可信：用户提供、实际验证、官方文档
- 🟡 需验证：模式推断 → **MUST** 先验证
- 🔴 禁止：纯假设、臆想

---

## 📋 每次操作前强制自检（MANDATORY）

**在thinking中必须检查：**

1. [ ] **CRITICAL** 我是否基于未经验证的假设？
2. [ ] **CRITICAL** 这是执行类任务还是回答类问题？
3. [ ] **CRITICAL CRITICAL** 是否有2个以上方案需要用户选择？
4. [ ] **CRITICAL** 需要选择时是否用了AskQuestion工具？（禁止聊天列选项！）
5. [ ] **🔴 CRITICAL** 我制定了方案吗？是否已用AskQuestion征求确认？（禁止直接执行！）
6. [ ] 我是否要生成报告/清单文件？（禁止）
7. [ ] 对比型问题是否聚焦差异？
8. [ ] 我是否已验证所有信息？
9. [ ] 我是否100%确定？

**任何"否"或"不确定"→ STOP重新检查**

---

## 🎯 违规示例（学习用）

### ❌ 错误1：臆想
```
错："表应该有user_id字段" → 正：DESCRIBE确认
```

### ❌ 错误2：聊天列选项（最常见错误！）
```
错："你的选择？
     A. 方案1
     B. 方案2" 
     
→ 正：AskQuestion({
  questions: [{
    id: "choice",
    options: [
      {id: "a", label: "方案1"},
      {id: "b", label: "方案2"}
    ]
  }]
})
```

### ❌ 错误3：生成报告
```
错：创建"报告.md" → 正：在回复中总结
```

### ❌ 错误4：直接执行方案（NEW - 常见错误！）
```
错："我将采用方案A，现在开始修改..." [同时执行]
     
→ 正："建议采用方案A：
      步骤1: ...
      步骤2: ...
      影响: ...
      是否确认执行？" [使用AskQuestion]
```

### ❌ 错误5：分析正常部分
```
用户："旧版正常，新版报错"
错：解释旧版为何正常 → 正：对比差异
```

---

## 🔄 规则优先级（MANDATORY）

```
00-CORE-ENFORCEMENT (本规则) 🔴 最高优先级 CRITICAL
  ↓
核心原则规则 ⚠️ MANDATORY
  ↓
其他细节规则 ℹ️ IMPORTANT
```

---

## 🔴 特别强调（再次提醒）

**关于AskQuestion规则：**
- 这是**最容易违反**的规则
- 必须在**thinking中强制检查**
- 看到多个方案→立即想到AskQuestion
- **永远不要**在聊天中列"1. 2. 3."或"A. B. C."

**记住：本规则是核心精华。每次操作前在thinking中检查。违反即严重错误。**
