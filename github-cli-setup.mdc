---
description: GitHub CLI 安装、代理配置与全局规则仓库同步
globs:
alwaysApply: false
---

# GitHub CLI 连接与全局规则管理 Skill

## 概述

本 Skill 封装了 GitHub CLI 的安装、代理网络下的认证、以及从 GitHub 仓库加载/同步 Cursor 全局规则的完整流程。

## 一、环境准备

### 1.1 检测 GitHub CLI 是否已安装

```bash
gh --version
```

若提示 `command not found`，执行安装流程。

### 1.2 安装 GitHub CLI（Windows + 代理环境）

直接使用 winget 在代理环境下可能失败，推荐通过 curl 代理下载后本地安装：

```bash
# 1. 通过代理下载 MSI 安装包（版本号按需替换）
curl -L -x http://127.0.0.1:7897 -o /tmp/gh_installer.msi \
  "https://github.com/cli/cli/releases/latest/download/gh_<VERSION>_windows_amd64.msi"

# 2. 以管理员权限静默安装
powershell -Command "Start-Process msiexec.exe -ArgumentList '/i','C:\Users\admin\AppData\Local\Temp\gh_installer.msi','/quiet','/norestart' -Wait -Verb RunAs"

# 3. 添加到 PATH（Git Bash 环境）
export PATH="/c/Program Files/GitHub CLI:$PATH"

# 4. 验证
gh --version
```

### 1.3 代理配置

在每次使用 `gh` 前，必须设置代理环境变量（Clash 默认端口根据实际情况调整）：

```bash
export HTTPS_PROXY=http://127.0.0.1:7897
export HTTP_PROXY=http://127.0.0.1:7897
```

**检测可用代理端口**：

```bash
# 测试常见 Clash 端口
curl -s --connect-timeout 3 -x http://127.0.0.1:7890 https://www.google.com -o /dev/null -w "%{http_code}"
curl -s --connect-timeout 3 -x http://127.0.0.1:7897 https://www.google.com -o /dev/null -w "%{http_code}"
```

返回 `200` 或 `302` 的端口即为可用代理端口。

## 二、GitHub 认证

### 2.1 浏览器交互登录（推荐）

```bash
export PATH="/c/Program Files/GitHub CLI:$PATH"
export HTTPS_PROXY=http://127.0.0.1:7897
export HTTP_PROXY=http://127.0.0.1:7897

gh auth login --web --git-protocol https
```

执行后会显示一次性代码和 URL：
1. 在浏览器打开 https://github.com/login/device
2. 输入显示的一次性代码
3. 授权 GitHub CLI 访问

### 2.2 验证登录状态

```bash
gh auth status
```

应显示 `Logged in as <username>`。

## 三、全局规则仓库操作

### 3.1 仓库信息

- **仓库地址**: `https://github.com/azrael-hao/cursor-global-rules`
- **本地全局规则目录**:
  - Windows: `%USERPROFILE%\.cursor\rules\`
  - macOS/Linux: `~/.cursor/rules/`

### 3.2 从仓库加载规则到本地（首次）

```bash
cd ~/.cursor/rules/
git init
git remote add origin https://github.com/azrael-hao/cursor-global-rules.git

export HTTPS_PROXY=http://127.0.0.1:7897
git pull origin master
```

### 3.3 从仓库同步最新规则（后续更新）

```bash
cd ~/.cursor/rules/
export HTTPS_PROXY=http://127.0.0.1:7897
git pull origin master
```

### 3.4 上传本地新规则到仓库

```bash
cd ~/.cursor/rules/
git add -A
git commit -m "Add/update rule: <规则名称>"

export HTTPS_PROXY=http://127.0.0.1:7897
git push origin master
```

### 3.5 使用 gh CLI 快速操作

```bash
export PATH="/c/Program Files/GitHub CLI:$PATH"
export HTTPS_PROXY=http://127.0.0.1:7897
export HTTP_PROXY=http://127.0.0.1:7897

# 查看仓库
gh repo view azrael-hao/cursor-global-rules

# 列出文件
gh api repos/azrael-hao/cursor-global-rules/contents
```

## 四、规则文件格式

每个 `.mdc` 文件必须包含 YAML front-matter：

```yaml
---
description: 规则简要描述
globs:              # 留空表示不限文件
alwaysApply: true   # true=所有项目自动生效, false=按需引用
---
```

## 五、完整操作速查

```bash
# === 一键设置环境 ===
export PATH="/c/Program Files/GitHub CLI:$PATH"
export HTTPS_PROXY=http://127.0.0.1:7897
export HTTP_PROXY=http://127.0.0.1:7897

# === 检查状态 ===
gh auth status
gh --version

# === 同步规则 ===
cd ~/.cursor/rules/ && git pull origin master

# === 上传更新 ===
cd ~/.cursor/rules/ && git add -A && git commit -m "Update rules" && git push origin master
```
