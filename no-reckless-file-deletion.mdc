---
description: 🔴 禁止粗暴删除文件 - 删除前必须确认影响、提供理由、征得同意
globs:
alwaysApply: true
---

# 🔴 禁止粗暴删除文件（NO RECKLESS FILE DELETION）

## 核心原则

**严禁随意、粗暴地删除项目中的已有文件。删除任何文件前必须充分评估影响、提供充分理由、征得用户明确同意。**

## 🚫 严禁的删除行为

### 1. 未经确认的删除
```bash
❌ 错误示例：
# 直接删除文件，没有询问用户
rm old_module.py
git rm legacy_feature.js
```

### 2. 批量盲目删除
```bash
❌ 错误示例：
# 删除所有"看起来"没用的文件
rm -rf backup_*
find . -name "*.old" -delete
```

### 3. 因理解不了而删除
```bash
❌ 错误示例：
# 这个文件看不懂，应该没用，删了吧
rm complex_algorithm.c
```

### 4. 为了"整洁"而删除
```bash
❌ 错误示例：
# 这些测试文件太多了，删除一些吧
rm tests/test_legacy_*.py
```

### 5. 因报错而删除
```bash
❌ 错误示例：
# 这个文件import报错，删掉算了
rm problematic_module.py
```

## ✅ 正确的文件删除流程

### Step 1: 识别文件用途

```
必须回答：
1. 这个文件是做什么的？
2. 哪些地方引用了这个文件？
3. 这个文件是否是某个功能的一部分？
4. 删除后会影响什么功能？
```

**验证方法**：
```bash
# 搜索文件引用
grep -r "filename" .
rg "import.*filename" --type py
rg "require.*filename" --type js

# 查看git历史
git log --follow -- path/to/file
git blame path/to/file
```

### Step 2: 评估删除影响

```
影响分析清单：
[ ] 代码层面：是否有其他文件依赖？
[ ] 功能层面：是否影响现有功能？
[ ] 数据层面：是否包含重要数据或配置？
[ ] 历史层面：是否有历史价值或未来可能需要？
[ ] 文档层面：是否在文档中被引用？
```

### Step 3: 提供删除理由

必须明确说明：
1. **为什么要删除**：具体原因
2. **影响评估**：删除后的影响
3. **替代方案**：是否有替代文件或功能
4. **风险评估**：可能的风险和应对

### Step 4: 征得用户同意

**必须使用AskQuestion工具**：
```javascript
AskQuestion({
  questions: [{
    id: "confirm_deletion",
    prompt: "发现以下文件，建议删除：\n\n文件：xxx\n理由：xxx\n影响：xxx\n\n是否确认删除？",
    options: [
      { id: "delete", label: "确认删除" },
      { id: "keep", label: "保留文件" },
      { id: "review", label: "我需要先查看" }
    ]
  }]
})
```

### Step 5: 安全删除

```bash
✅ 正确做法：

# 1. 使用git rm（保留历史）
git rm path/to/file
git commit -m "Remove deprecated feature X - 理由: xxx"

# 2. 或者先备份再删除
mkdir -p .deleted_backup/$(date +%Y%m%d)
cp path/to/file .deleted_backup/$(date +%Y%m%d)/
rm path/to/file
```

## 📋 文件删除分类处理

### 类别1：可以删除的文件

以下文件在**明确确认后**可以删除：

1. **临时文件**
   ```bash
   *.tmp, *.temp, *.cache, *.bak
   __pycache__/, node_modules/, .DS_Store
   ```
   - 但仍需确认是否在.gitignore中已忽略

2. **明确废弃的文件**
   - 有明确的废弃标记
   - 已被新文件完全替代
   - 功能已正式下线
   - 需要在commit message中说明替代方案

3. **用户明确要求删除的文件**
   - 用户直接说"删除xxx文件"
   - 仍需提醒可能的影响

### 类别2：谨慎删除的文件

以下文件需要**特别谨慎**：

1. **配置文件**
   ```
   config.json, .env, settings.py
   ```
   - 可能包含重要配置
   - 删除前必须确认是否有备份

2. **数据文件**
   ```
   *.sql, *.db, *.csv, *.json (data)
   ```
   - 可能包含重要数据
   - 必须确认有完整备份

3. **文档文件**
   ```
   README.md, *.doc, *.pdf
   ```
   - 可能包含重要说明
   - 删除前需要评估信息是否已转移

4. **测试文件**
   ```
   test_*.py, *.test.js, *.spec.ts
   ```
   - 可能是重要的测试用例
   - 删除前需确认功能是否仍需要测试

### 类别3：禁止删除的文件

以下文件**绝对不能**随意删除：

1. **核心业务代码**
   - 主要功能模块
   - 关键算法实现

2. **依赖声明文件**
   ```
   package.json, requirements.txt, Gemfile
   pom.xml, build.gradle, Cargo.toml
   ```

3. **版本控制文件**
   ```
   .git/, .gitignore, .gitattributes
   ```

4. **系统关键文件**
   ```
   .env (生产环境), credentials, certificates
   ```

5. **不理解的文件**
   - 如果不确定文件用途，**绝对不能删除**
   - 应该先询问用户或搜索引用

## 🎯 特殊场景处理

### 场景1：重构时的文件删除

```
正确流程：
1. 创建新文件实现新功能
2. 迁移所有引用到新文件
3. 运行测试确保功能正常
4. 标记旧文件为 @deprecated（保留一段时间）
5. 一段时间后再删除旧文件
6. 删除时更新相关文档
```

### 场景2：发现重复文件

```
不要直接删除！应该：
1. 对比两个文件的差异（diff）
2. 确认功能是否完全相同
3. 查看引用情况
4. 如果有细微差异，分析原因
5. 统一引用到保留的文件
6. 征得用户同意后删除
```

### 场景3：清理"无用"代码

```
❌ 错误：认为没有引用就可以删除
✅ 正确：
1. 使用静态分析工具（如pylint, ESLint）
2. 查看git历史，确认最近是否使用
3. 考虑是否被反射、动态加载等方式使用
4. 在开发环境测试删除后的影响
5. 提供清理清单给用户审核
```

### 场景4：多个"类似"文件

```
例如：
- backup_20230101.sql
- backup_20230201.sql
- backup_20230301.sql
...

不要自作主张"只保留最新的"！
应该：
1. 列出所有文件及其大小、时间
2. 询问用户的备份策略
3. 是否需要归档而非删除
4. 征得同意后按策略处理
```

## 📝 删除前自检清单（MANDATORY）

在删除**任何**文件前，必须完成以下检查：

```
=== 文件删除自检 ===

【文件信息】
- 文件路径：_______________
- 文件大小：_______________
- 创建时间：_______________
- 最后修改：_______________

【用途分析】
- [ ] 我知道这个文件是做什么的
- [ ] 我已搜索了所有引用（grep/rg）
- [ ] 我已查看了git历史（git log）
- [ ] 我理解了删除的影响

【影响评估】
- [ ] 不会影响现有功能
- [ ] 不会导致编译/运行错误
- [ ] 不会丢失重要数据
- [ ] 不会影响其他开发者

【删除理由】
- 理由：_______________
- 替代方案：_______________
- 风险评估：_______________

【用户确认】
- [ ] 已使用AskQuestion征得用户同意
- [ ] 用户明确说"可以删除"

=== 检查完毕 ===
```

**如果任何一项为"否"或"不确定"→ 不能删除，必须先调查清楚**

## 🚨 违规示例

### 示例1：直接删除"看起来没用"的文件

```
❌ 错误思考过程：
"这个legacy_utils.py文件名字有legacy，应该是遗留代码，没人用了，删掉吧"

✅ 正确思考过程：
"这个文件名字有legacy，但：
1. 让我先搜索引用：rg 'legacy_utils' --type py
2. 发现在3个地方被import
3. 查看git历史：最近2周还有改动
4. 结论：虽然名字叫legacy但仍在使用，不能删除"
```

### 示例2：批量删除测试文件

```
❌ 错误：
"项目有100个测试文件太多了，删除一些吧"

✅ 正确：
"发现项目有100个测试文件，但：
1. 测试文件是保证代码质量的关键
2. 不能因为数量多就删除
3. 如果有重复或无效的测试，应该具体分析
4. 需要逐个审查，不能批量删除"
```

### 示例3：因报错而删除

```
❌ 错误：
"import这个文件报错ModuleNotFoundError，文件有问题，删掉"

✅ 正确：
"import报错不代表文件有问题，可能是：
1. 依赖没安装
2. 路径配置问题
3. 环境问题
应该解决import问题，而不是删除文件"
```

## 💡 替代删除的方案

如果不确定是否应该删除，可以考虑：

### 方案1：移动到归档目录
```bash
mkdir -p _archived/$(date +%Y%m%d)
git mv old_file.py _archived/$(date +%Y%m%d)/
```

### 方案2：添加弃用标记
```python
# deprecated.py
"""
@deprecated 此模块已废弃，请使用 new_module.py
计划删除时间：2026-12-31
替代方案：new_module.py
"""
```

### 方案3：注释说明
```javascript
// TODO: 此文件可能已不再使用，需要进一步确认
// 引用搜索：无直接引用（2026-05-09）
// 保留原因：等待用户确认
```

## 🎓 正确示例

### 示例1：正确的删除流程

```
1. 发现文件：old_api.py
2. 搜索引用：rg "old_api" --type py
   结果：无引用
3. 查看历史：git log --follow old_api.py
   结果：2年前创建，已被new_api.py替代
4. 查看new_api：确认功能已完全迁移
5. 使用AskQuestion征求用户意见
6. 用户同意后：git rm old_api.py
7. 提交：git commit -m "Remove deprecated old_api.py, replaced by new_api.py"
```

### 示例2：发现不应删除的情况

```
1. 发现文件：utils_legacy.py
2. 搜索引用：rg "utils_legacy"
   结果：在payment_processor.py中被import
3. 结论：虽然名字有legacy，但仍被核心模块使用
4. 决定：保留文件，不删除
5. 如有必要，可以重命名或重构，但不是删除
```

---

**记住：删除是不可逆的操作。谨慎删除，永远不后悔。鲁莽删除，可能造成严重后果。**

**核心口诀：看不懂不删，有引用不删，不确定不删，未经允许不删。**
