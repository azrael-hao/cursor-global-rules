---
description: 🔴 CRITICAL - 必须解决根本原因 - 禁止压制、禁止绕过、禁止适应错误环境
globs:
alwaysApply: true
---

# 🔴 解决根本原因强制规范

> **通用原则：始终解决根本原因。绝不压制问题、用workaround绕过、或在未经用户批准的情况下适应错误环境。**

## 🚫 绝对禁止 - 三种反模式

### 1. 压制（隐藏问题）

**严格禁止：**
- ❌ 删除抛出错误的代码
- ❌ 捕获异常后吞掉
- ❌ 注释掉失败的测试
- ❌ 禁用linter警告
- ❌ 删除验证逻辑
- ❌ 手动修改数据以适应代码
- ❌ 降低安全标准
- ❌ 绕过权限检查

### 2. 绕过（用workaround避免问题）

**严格禁止：**
- ❌ 跳过实际问题的解决，转而建议替代方案
- ❌ 提出workaround而不修复根本问题
- ❌ 修改需求使其"更容易"实现
- ❌ 未经用户同意切换解决方案
- ❌ 用"太复杂"作为借口避免解决

### 3. 适应错误环境

**严格禁止：**
- ❌ 降低需求以匹配错误环境（如Java 21→11）
- ❌ 修改方案以绕过环境问题
- ❌ 仅因环境不支持就删除功能
- ❌ 接受错误/过时环境为不可变约束
- ❌ 选择"简单但错误"而非"复杂但正确"

## ✅ 强制执行 - 正确的问题解决流程

### 步骤1：诊断根本原因（见diagnose-before-action规则）
1. 究竟什么失败了？
2. 在哪里失败？
3. 根本原因是什么？
4. 需求为何存在？

### 步骤2：识别正确解决方案
```
优先级1：直接修复根本原因
优先级2：如果根因解决方案困难 → 使用AskQuestion
优先级3：任何workaround/适应前获得用户批准
优先级4：禁止单方面选择workaround
```

### 步骤3：实施并验证
1. 实施针对根本原因的修复
2. 验证问题不再发生
3. 验证没有引入新问题
4. 确认解决方案可持续（不会再次出现）

## 强制使用AskQuestion的场景

使用AskQuestion工具当：

1. **根因解决方案非常困难** - 需要大量工作/资源
2. **存在多种有效方法** - 需要用户选择偏好的解决方案
3. **考虑任何workaround** - 不修复根因的替代方案
4. **环境修复看起来"太难"** - 想要降级需求
5. **存在权衡** - 快速修复vs正确修复、性能vs可维护性

### AskQuestion模板

```javascript
AskQuestion({
  title: "问题解决方案选择",
  questions: [{
    id: "approach",
    prompt: "问题：[描述问题]
             根本原因：[精确原因]
             
             方案A：[修复根本原因]（推荐）
               - 永久解决
               - 尊重设计意图
               - 可能需要：[工作量/资源]
             
             方案B：[workaround/适应]
               - 不修复根本原因
               - 风险：[列出风险]
               - 考虑原因：[原因]
             
             您偏好哪种方案？",
    options: [
      { id: "fix", label: "修复根本原因（方案A - 推荐）" },
      { id: "workaround", label: "使用workaround（方案B）" }
    ]
  }]
})
```

## 按类别分类的通用示例

### 类别1：压制示例

❌ **错误**：
```
问题：函数抛出错误
操作：try { riskyFunction() } catch (e) { /* ignore */ }
```

✅ **正确**：
```
问题：函数抛出错误
操作：调查为何抛出 → 修复原因 → 移除try-catch需求
```

### 类别2：绕过示例

❌ **错误**：
```
问题：查询超时
操作：减少数据范围以避免超时
```

✅ **正确**：
```
问题：查询超时
根本原因：缺少索引
操作：添加索引 → 查询现在对完整数据也很快
```

### 类别3：环境适应示例

❌ **错误**：
```
问题：代码要求Java 21，环境只有Java 11
操作：降级pom.xml到Java 11
```

✅ **正确**：
```
问题：代码要求Java 21，环境只有Java 11
根本原因：环境过时
操作：安装Java 21 → 代码按设计工作
```

### 类别4：多类别综合示例

❌ **错误 - 所有三种反模式**：
```
问题：数据库字符集是latin1，导致emoji错误
操作（全错）：
- 压制：捕获UnicodeError并跳过emoji
- 绕过：将emoji转换为文本":smile:"
- 适应：从应用中删除emoji支持
```

✅ **正确**：
```
问题：数据库字符集是latin1，导致emoji错误
根本原因：字符集配置错误
操作：修改数据库字符集为utf8mb4 → emoji正常工作
```

## 判断标准 - 行动前检查

问自己：
- [ ] 我是否已识别出精确的根本原因？
- [ ] 我是否准备压制/绕过/适应错误环境？
- [ ] 我是否应该修复根本原因？
- [ ] 我是否在选择"简单错误"而非"困难正确"？
- [ ] 如考虑workaround：我是否已通过AskQuestion询问用户？

**如果任何答案是"对错误方法说是"→ 停止并重新考虑**

## 核心口诀

1. **"优秀工程师解决问题，糟糕工程师隐藏问题"**
2. **"问题→解决→成功，而非：问题→避免→不同路径"**
3. **"修复环境，而非降低需求"**
4. **"尊重原始设计意图"**
5. **"简单的错误比困难的正确更糟糕"**

## 无需询问的允许场景

✅ **直接修复根本原因** - 不涉及workaround
✅ **修复时改进** - 在修复过程中使解决方案更好
✅ **添加安全检查** - 防止将来出现类似问题
✅ **修复多个根本原因** - 如果问题有2个以上原因，全部修复

---

*本规则整合：solve-not-suppress、no-workaround-without-approval、fix-root-cause-not-adapt*
