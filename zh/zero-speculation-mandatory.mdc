---
description: 🔴 CRITICAL - 零臆想强制执行 - 禁止假设、禁止推断、数据驱动 - thinking中必须自检
globs:
alwaysApply: true
---

# 🔴 零臆想强制执行

> **统一验证原则**：零臆想、零假设、零推断、数据驱动。

## 🚫 严禁

### 臆想与假设
1. **臆想**：假设文件/方法/字段/变量/数据结构/类型存在
2. **预测**：预测代码行为/性能/数据/错误（必须实际运行/测试/查询）
3. **猜测**：凭感觉判断、凭经验猜测（未验证）、理论推断（未证实）
4. **假设**：假设数据结构/类型/格式、业务逻辑/流程/规则
5. **推断**：根据命名推断功能、根据类型推断行为、根据模式推断结构

## 🔴 thinking强制自检（MANDATORY）

```
=== 零臆想自检 ===

【步骤1】我现在要做什么？

【步骤2】臆想风险检查
- [ ] 是否假设了某个文件/方法/字段存在？
- [ ] 是否假设了数据结构或类型？
- [ ] 是否基于"应该"、"可能"、"大概"判断？
- [ ] 是否未查看实际代码/数据就做决策？

【步骤3】需验证内容
1. _________________________
2. _________________________

【步骤4】声明
✅ 本次操作基于：
- 实际查看的文件：___________
- 实际查询的数据：___________
- 实际执行的验证：___________

=== 自检结束 ===
```

## ✅ 强制验证

### 各场景验证要求
| 场景 | 禁止 | 必须 |
|------|------|------|
| 代码修改 | 假设方法存在 | Grep搜索定义 → Read代码 → 确认签名 |
| 数据库操作 | 假设字段存在 | DESCRIBE table → 确认字段和类型 |
| API调用 | 假设返回格式 | Read API定义 → 确认返回类型 |
| 错误诊断 | 猜测错误原因 | Read错误日志 → 查看代码 → 确认原因 |
| 性能优化 | 假设瓶颈 | EXPLAIN ANALYZE → 确认瓶颈 |

## 🚨 禁用词汇

❌ "应该"、"可能"、"大概"、"估计"、"理论上"、"按照惯例"、"一般来说"、"差不多"、"预计"

✅ 替代："让我查看"、"让我验证"、"让我确认"

## 📊 数据驱动决策场景

| 场景 | 错误做法 | 正确做法 |
|------|---------|---------|
| 性能优化 | "这个查询应该很慢" | EXPLAIN ANALYZE查看实际执行计划 |
| 数据规模 | "表数据不多" | SELECT COUNT(*) 获取实际行数 |
| 索引效果 | "加索引应该会快" | 对比加索引前后实际性能 |
| 资源使用 | "内存使用不高" | 查看监控数据（CPU、内存、QPS） |
| 容量规划 | "明年大概需要xxx" | 查询历史增长数据 |

## 可信vs不可信信息

**可信（无需验证）**：
- 用户明确提供的信息
- 刚刚查询/读取的结果
- 当前对话中已验证的内容

**不可信（必须验证）**：
- 记忆中的代码结构
- 经验推断的内容
- 理论知识的应用

## 核心原则

1. **零假设**：绝不假设任何东西存在或以某种方式工作 → 必须验证
2. **零推断**：绝不从命名或类型推断行为 → 必须查看实现
3. **数据驱动**：绝不估算或猜测 → 必须获取真实数据
4. **验证优先**：Read、Grep、DESCRIBE、Query 后再决策

**核心口诀：先验证，后执行。无验证，不操作。**

---

*本规则整合：zero-speculation-mandatory、no-assumption-core、no-inference-verification-required、data-driven-decisions*
