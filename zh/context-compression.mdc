---
description: 上下文压缩策略 - 接近容量上限时触发。压缩对话历史的同时保持10个核心规则完整。
globs:
alwaysApply: true
---

# 上下文压缩（Context Compression）

## 触发条件

感知到以下任一信号表明上下文接近耗尽时，必须立即压缩：

1. 对话轮数超过20轮且有大量工具调用结果
2. 读取或生成的代码/数据块累计超过2000行
3. 子任务（Task/subagent）运行超过5分钟仍未完成
4. 感觉响应被截断或无法完整生成

## 压缩策略

触发后执行以下步骤：

### 1. 主动告知用户

在响应开头清晰说明：
> "上下文接近容量上限，正在压缩以保持会话连续性。"

### 2. 生成结构化摘要

将当前会话关键信息压缩为以下结构：

- **当前任务**：一句话描述正在做什么
- **已完成**：已完成的步骤列表
- **待完成**：未完成的步骤列表
- **关键文件**：涉及的文件路径及其作用
- **关键决策**：做出的技术决策及理由
- **等待确认**：需要用户确认的问题

### 3. 避免重复读取

压缩后，不要重新读取已处理的文件内容，只引用文件路径。对于大型数据结果，只保留统计摘要而非原始数据。

### 4. 拆分大任务

如果任务太大无法在单次会话中完成：
- 将剩余工作拆分为可独立执行的子步骤
- 使用TODO列表跟踪进度
- 优先使用脚本自动化（Python/Shell）而非逐行手动操作
- 避免对生成大量文本的任务使用subagent，改用本地脚本

## 🔴 关键：规则永不压缩

**10个核心规则必须保持完整和可访问：**
1. 00-CORE-ENFORCEMENT（包含5个辅助指引）
2. zero-speculation-mandatory（统一验证原则）
3. user-interaction-mandatory（统一用户交互规则）
4. diagnose-before-action
5. solve-not-suppress
6. single-turn-resolution
7. no-unnecessary-divergence
8. no-reckless-file-deletion
9. adaptive-thinking
10. context-compression

**压缩时禁止：**
- ❌ 压缩或总结规则内容
- ❌ 从上下文中移除规则
- ❌ 用摘要替代完整规则

**压缩时必须：**
- ✅ 保持所有10个核心规则完整可访问
- ✅ 在摘要中明确说明"所有规则保持完整"

## 预防措施

在整个对话过程中，遵循以下原则减缓上下文消耗：

- 读取文件时，优先使用offset/limit参数，避免一次读取超过200行
- 对于过长的SSH命令输出，使用`head`/`tail`截断，将完整结果保存到文件
- 生成大型文档时，使用Python脚本而非逐行手动编写
- 工具调用结果超过100行时，提取关键信息，不要再次引用原始输出
- 每个里程碑后更新TODO列表，确保进度可追溯
