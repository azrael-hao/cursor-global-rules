---
description: 修复根本原因而非适应错误环境 - 通用原则
globs:
alwaysApply: true
---

# 修复根本原因，而非适应错误环境

> **通用原则：面对任何问题时，修复根本原因（环境/配置/依赖），而非修改方案去适应错误环境。**

## 🚫 绝对禁止 - 通用反模式

**面对任何类型问题时严禁：**
- ❌ **降低要求以匹配错误环境**
- ❌ **修改方案以绕过环境问题**
- ❌ **选择"简单但错误"而非"复杂但正确"**
- ❌ **假设修复环境"对用户太麻烦"**
- ❌ **仅因环境不支持就删除功能/特性**
- ❌ **接受错误/过时环境为不可变约束**

## ✅ 强制执行 - 通用原则

### 1. 问题分析 - 第一性原理

**任何问题发生时，识别：**
1. **什么出错了？**（需求 vs 现实 不匹配）
2. **根本原因在哪？**（环境、配置、依赖、权限等）
3. **应该修复什么？**（根本原因，而非需求）
4. **需求为何存在？**（设计意图、功能需要、最佳实践）

### 2. 决策优先级 - 通用层次

**始终遵循此优先级：**
```
优先级1：修复根本原因（环境、配置、依赖）
优先级2：询问用户偏好哪种方案
优先级3：如用户坚持绕过，记录风险
优先级4：禁止单方面选择绕过方案
```

### 3. 正确的推理模式

❌ **错误思维模式**：
```
"X无法工作因为环境缺少Y，
让我修改X使其不需要Y"
```

✅ **正确思维模式**：
```
"X无法工作因为环境缺少Y，
这意味着：
1. X设计时使用Y是有充分理由的
2. 移除Y要求可能破坏功能
3. 应该修复环境以提供Y
4. 必须在改变方案前询问用户"
```

## 通用示例

### 类别1：版本不匹配
```
问题：代码要求Java 21，环境只有Java 11
❌ 错误：降级代码到Java 11
✅ 正确：安装Java 21

问题：代码要求Node 20，环境只有Node 16
❌ 错误：重写代码适配Node 16
✅ 正确：安装Node 20
```

### 类别2：依赖缺失
```
问题：代码需要库X，但未安装
❌ 错误：重写代码避免使用库X
✅ 正确：安装库X

问题：系统缺少包Y导致功能Z不可用
❌ 错误：删除功能Z
✅ 正确：安装包Y
```

### 类别3：配置问题
```
问题：数据库字符集是latin1，代码需要utf8mb4
❌ 错误：修改代码使用latin1
✅ 正确：修改数据库字符集为utf8mb4

问题：服务器时区错误，导致时间计算错误
❌ 错误：在代码中添加workaround计算
✅ 正确：修正服务器时区配置
```

### 类别4：权限/访问问题
```
问题：代码需要/var/log写权限
❌ 错误：修改代码跳过日志记录
✅ 正确：授予适当权限或更改日志路径

问题：API需要认证token
❌ 错误：在代码中移除认证要求
✅ 正确：配置正确的认证
```

### 类别5：性能/资源问题
```
问题：大数据集导致查询慢
❌ 错误：人为减少数据集
✅ 正确：添加索引、优化查询、扩展资源

问题：内存限制过低无法完成操作
❌ 错误：降低功能以适应限制
✅ 正确：提高内存限制或优化内存使用
```

### 类别6：网络/连接问题
```
问题：防火墙阻止必需端口
❌ 错误：修改代码使用不同协议
✅ 正确：配置防火墙允许端口

问题：需要代理才能访问外部
❌ 错误：删除外部依赖
✅ 正确：配置代理设置
```

## 核心口诀

1. **"修复环境，而非降低要求"**
2. **"尊重原始设计意图"**
3. **"不为环境便利而妥协方案质量"**
4. **"简单的错误比困难的正确更糟糕"**
5. **"环境应该适应需求，而非需求适应错误环境"**

## Thinking中必检项

遇到任何问题时：
- [ ] 我是否已识别根本原因？
- [ ] 我是否准备让方案适应错误环境？
- [ ] 我是否应该修复环境？
- [ ] 我是否已询问用户偏好哪种方案？
- [ ] 我是否在选择"简单错误"而非"困难正确"？

**如果任何答案暗示"适应错误环境"→ 停止并使用AskQuestion**

---

*这是适用于所有问题的通用原则，不仅限于代码问题*
