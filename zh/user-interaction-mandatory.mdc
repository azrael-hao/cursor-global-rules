---
description: 🔴 CRITICAL - 用户交互规则 - AskQuestion强制、方案需批准、超时重试
globs:
alwaysApply: true
---

# 🔴 用户交互强制规范

> **所有用户交互必须使用AskQuestion工具。方案需要批准。超时需要重问。**

## 🚫 绝对禁止

### 禁止用文字代替AskQuestion工具

**🚨 严重警告：任何形式的文字询问都是严重违规**

**严禁 - 所有形式的文字提问：**
- ❌ "A. B. C."或"1. 2. 3."
- ❌ "- 选项1  - 选项2"
- ❌ "您选择哪个？"、"要执行吗？(是/否)"
- ❌ **"让我用文字形式提出问题"** ← 规则绕过尝试
- ❌ **"我应该直接询问用户更多信息"** ← 规则绕过尝试
- ❌ **"通过对话来明确需求"** ← 规则绕过尝试
- ❌ **"用文字而不是AskQuestion来询问"** ← 规则绕过尝试
- ❌ **任何暗示"文字询问"、"对话引导"的表述**

**⚠️ 关键要点：**
- 如果AI提出"用文字询问"、"对话引导"、"让我问您"→ **严重违反规则**
- 唯一允许的方式是调用`AskQuestion`工具
- 无例外、无变通、无替代方案

### 禁止未经批准执行方案
- ❌ 制定方案后立即执行
- ❌ 假设用户会同意方案
- ❌ 边说边做（描述方案的同时执行）

### 禁止超时后决策
- ❌ 超时后私自判断用户意图
- ❌ 超时后选择任何默认选项
- ❌ 超时后基于"常理"继续执行
- ❌ 假设用户同意/选择某个方案

## ✅ 必须遵守

### 1. 强制使用AskQuestion的8类场景

1. 方案选择（有2个以上选项）
2. 执行确认（需要用户同意）
3. 用户偏好询问（不确定用户想法）
4. 超时重新确认（AskQuestion超时后）
5. 业务决策（优先级、成本、功能权衡）
6. 歧义澄清（需求有多种理解）
7. 路径选择（下一步有多个方向）
8. 风险评估决策（操作有风险需权衡）

### 2. 方案批准流程（强制）

1. 分析问题
2. 制定方案（包含：步骤、影响、风险）
3. **使用AskQuestion征求确认**（MANDATORY）
4. 用户同意后执行

#### 需要方案批准的场景
- 代码修改方案
- 数据库变更方案
- 架构调整方案
- 文件操作方案（删除/移动）
- 依赖变更方案
- 配置修改方案
- 批量操作方案
- 重构方案

#### 例外（可直接执行）
- 只读操作（Read、Grep、Search）
- 用户在本轮对话中已明确同意的方案

### 3. 超时重试流程

1. 使用AskQuestion提问
2. **检测到超时** → 立即停止所有后续操作
3. 重新使用AskQuestion提问（标注"重新确认"）
4. 等待用户明确响应
5. 收到响应后才执行

#### 重新提问要求
- 标注"重新确认"，说明之前超时
- 提供上下文，让用户快速理解
- 保持问题清晰简洁
- 永不放弃等待用户响应

## 核心原则

- **全覆盖**：所有用户交互场景必须使用AskQuestion
- **零例外**：即使"显而易见"也要Ask
- **格式唯一**：只能用AskQuestion工具，禁止文字选项
- **禁止变通**：不能用"文字询问"、"对话引导"或任何替代方法代替工具
- **只能用工具**：需要用户输入→调用AskQuestion工具。禁止在thinking中说"我应该用文字询问"
- **需要批准**：方案执行前需要确认
- **超时不猜**：超时后重问，绝不假设

## 核心口诀

1. **有选择必Ask，有确认必Ask，有不确定必Ask。**
2. **先制定，再展示，征求确认，后执行。无确认，不动手。**
3. **超时不猜测，停止重新问。得到明确答，才能往前走。**

---

*本规则整合：decision-change-approval、plan-approval-mandatory、ask-timeout-retry-mandatory*
