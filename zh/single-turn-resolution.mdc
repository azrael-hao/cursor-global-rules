---
description: 🔴 CRITICAL - 一次对话完整解决问题 - 禁止跨多次对话拆分任务
globs:
alwaysApply: true
---

# 🔴 一次对话完整解决（SINGLE-TURN RESOLUTION）

## 🚫 禁止

1. 留下半成品让用户说"继续"
2. 中途停下来让用户重新请求剩余步骤
3. 能一次收集的上下文分多次懒散获取
4. 把可一次解决的任务人为拆成多个"阶段"
5. 任务已明确时只输出方案却不执行
6. 问可以通过读代码解决的澄清问题

## ✅ 必须

1. **前置收集上下文**：动手前先读取所有相关文件、搜索引用、全面理解
2. **批量调用工具**：独立的读取/搜索操作并行执行，不要一个一个跨轮次
3. **执行到完成**：实现完整方案，不要停在"第1步/共3步"
4. **处理连锁变更**：如果修改需要更新多个文件，在同一轮全部完成
5. **变更后验证**：在同一轮内尽可能运行相关检查（lint、build、test）

## 判断标准

结束回复前自问：
- 用户的请求完全解决了吗？← **这是关键**
- 用户是否需要说"继续"或"接着做"？→ 如果是，你还没做完
- 是否有你已知的后续步骤？→ 现在就做

## 例外（允许跨轮次）

- 用户明确要求分阶段执行
- 需要AskQuestion确认（plan-approval-mandatory规则）
- 任务确实超出单次容量（告知用户后继续，无需用户重新提问）

**核心口诀：一次请求，一次完整交付。用户不应为同一件事开口两次。**
