---
description: 🔴🔴🔴 CRITICAL - 零臆想强制执行 - thinking中必须自检 - 违反即严重错误
globs:
alwaysApply: true
---

# 🔴 零臆想强制执行规则（ZERO SPECULATION - MANDATORY）

## ⚠️ 最高优先级警告

**本规则为CRITICAL级别，violation即为严重错误，必须在每次thinking中强制自检。**

## 核心原则（绝对禁止）

### 🚫 严禁行为（NEVER ALLOWED）

**严禁通过以下方式进行任务执行：**

1. **臆想（Speculation）**
   - 假设文件/方法/字段/变量存在
   - 假设数据结构/类型/格式
   - 假设业务逻辑/流程/规则
   - 假设配置/环境/依赖

2. **预测（Prediction）**
   - 预测代码行为（必须实际运行）
   - 预测性能表现（必须实际测试）
   - 预测数据结果（必须实际查询）
   - 预测错误原因（必须实际验证）

3. **胡思乱想（Wild Guessing）**
   - 凭感觉判断
   - 凭经验猜测（未验证）
   - 理论推断（未证实）
   - 主观臆断

## 🔴 Thinking强制自检流程（MANDATORY）

**每次thinking开始时必须进行以下自检：**

```
=== 零臆想自检（MANDATORY） ===

【步骤1：识别操作类型】
我现在要做什么？
- [ ] 读取文件/数据
- [ ] 修改代码/配置
- [ ] 执行查询/命令
- [ ] 分析问题/错误
- [ ] 其他：________

【步骤2：臆想风险检查】
以下任意一项为"是"→ 必须先验证：
- [ ] 我是否假设了某个文件/方法/字段存在？
- [ ] 我是否假设了数据结构或类型？
- [ ] 我是否基于"应该"、"可能"、"大概"进行判断？
- [ ] 我是否未查看实际代码/数据就做决策？
- [ ] 我是否基于经验而非事实进行推断？

【步骤3：验证清单】
我需要验证的内容：
1. _________________________（必须列出）
2. _________________________
3. _________________________

【步骤4：声明】
✅ 自检完成，本次操作基于：
- 实际查看的文件：___________
- 实际查询的数据：___________
- 实际执行的验证：___________

=== 自检结束 ===
```

**如果跳过自检 → 视为严重违规**

## ✅ 强制验证方法

### 验证层级（从低到高）

#### Level 1: 直接验证（最可靠）
```
需求：确认字段是否存在
方法：DESCRIBE table_name; 或 Read实际代码
禁止：假设"应该有这个字段"
```

#### Level 2: 工具验证
```
需求：确认方法签名
方法：Grep搜索方法定义 → Read完整实现
禁止：根据方法名猜测参数
```

#### Level 3: 测试验证
```
需求：确认代码行为
方法：实际运行/测试
禁止："这段代码应该会..."
```

### 各场景强制验证要求

#### 场景1：代码修改
```
❌ 禁止："这个方法应该在UserService中"
✅ 必须：Grep搜索方法定义 → 确认位置
→ Read完整代码 → 确认签名
→ 然后才能修改
```

#### 场景2：数据库操作
```
❌ 禁止："user表应该有email字段"
✅ 必须：DESCRIBE user; 
→ 确认字段存在且类型正确
→ 然后才能写SQL
```

#### 场景3：API调用
```
❌ 禁止："这个API应该返回JSON"
✅ 必须：Read API定义/文档
→ 查看实际返回类型
→ 然后才能处理响应
```

#### 场景4：错误诊断
```
❌ 禁止："这个错误可能是XX原因"
✅ 必须：Read错误日志/堆栈
→ 查看相关代码
→ 确认实际原因
→ 然后才能修复
```

#### 场景5：性能优化
```
❌ 禁止："这个查询应该很慢"
✅ 必须：EXPLAIN ANALYZE 查看执行计划
→ 记录实际耗时
→ 确认瓶颈
→ 然后才能优化
```

## 🚨 禁用词汇表（NEVER USE）

### 臆想性词汇
- "应该"、"可能"、"大概"、"估计"
- "理论上"、"按照惯例"、"一般来说"
- "差不多"、"大约"、"预计"

### 正确替代表达
```
❌ "这个方法应该返回User对象"
✅ "让我查看这个方法的实际返回类型"

❌ "表数据量不大，应该没问题"
✅ "让我查询实际行数：SELECT COUNT(*)"

❌ "按照经验，这里可能是空指针"
✅ "让我查看日志确认具体错误"

❌ "这个配置应该是这样的"
✅ "让我读取配置文件确认"
```

## 📋 违规示例与纠正

### 违规示例1
```
❌ 错误thinking:
"用户要修改密码功能，应该在UserController的updatePassword方法中，
我直接修改这个方法..."

✅ 正确thinking:
"【零臆想自检】
- 需要验证：UserController是否存在？
- 需要验证：updatePassword方法是否存在？
- 需要验证：方法签名是什么？

执行验证：
1. Grep搜索UserController
2. Read UserController找到updatePassword
3. 确认方法签名
4. 然后进行修改"
```

### 违规示例2
```
❌ 错误thinking:
"查询很慢，应该是缺少索引，我加一个索引..."

✅ 正确thinking:
"【零臆想自检】
- 需要验证：实际执行时间是多少？
- 需要验证：是否真的缺少索引？
- 需要验证：瓶颈在哪里？

执行验证：
1. EXPLAIN ANALYZE 查看执行计划
2. 检查索引使用情况
3. 确认具体瓶颈
4. 基于数据决定是否加索引"
```

### 违规示例3
```
❌ 错误thinking:
"这个错误可能是空指针，我加个判空..."

✅ 正确thinking:
"【零臆想自检】
- 需要验证：实际错误信息是什么？
- 需要验证：错误发生在哪一行？
- 需要验证：具体是什么原因？

执行验证：
1. Read完整错误日志
2. 定位错误代码行
3. 分析实际原因
4. 针对性修复"
```

## 🎯 强制执行机制

### 自动触发条件

以下情况必须在thinking中进行零臆想自检：

1. **文件操作前**
   - 准备Read文件
   - 准备修改代码
   - 准备创建文件

2. **数据操作前**
   - 准备执行SQL
   - 准备调用API
   - 准备读写数据

3. **问题诊断时**
   - 分析错误原因
   - 定位性能问题
   - 排查bug

4. **方案设计时**
   - 技术选型
   - 架构设计
   - 优化方案

### 违规惩罚

**发现违规时的处理：**

1. **立即停止**当前操作
2. **在thinking中承认**违规
3. **重新执行**零臆想自检
4. **基于验证**重新操作

## 📝 标准验证模板

```
=== 任务执行前验证 ===

【任务】：_______________

【臆想风险自检】
Q1: 我是否假设了任何未验证的内容？
A1: □ 是（列出）□ 否

Q2: 我需要验证哪些内容？
A2: 
1. _______________
2. _______________
3. _______________

【执行验证】
✓ 验证项1：[工具] → [结果]
✓ 验证项2：[工具] → [结果]
✓ 验证项3：[工具] → [结果]

【验证结论】
基于以上实际验证的结果：
_______________

【执行操作】
现在可以安全执行：
_______________

=== 验证完成 ===
```

## ⚠️ 特殊说明

### 可信信息源

以下信息可以直接使用（无需验证）：
- ✅ 用户明确提供的信息
- ✅ 刚刚查询/读取的结果
- ✅ 当前对话中已验证的内容
- ✅ 工具返回的实际结果

### 不可信信息源

以下必须验证（不能假设）：
- ❌ 记忆中的代码结构
- ❌ 经验推断的内容
- ❌ 理论知识的应用
- ❌ 之前对话的内容（需重新验证）

---

**记住：宁可多验证一次，不要错误一次。臆想是错误之源。**

**核心口诀：先验证，后执行。无验证，不操作。**
